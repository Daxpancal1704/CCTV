--- MATCH 0 ---
Created At: 2026-06-29T10:25:39Z
Completed At: 2026-06-29T10:25:39Z
File Path: `file:///c:/private/Project/SmartCCTV/dashboard/views.py`
Total Lines: 1776
Total Bytes: 60011
Showing lines 800 to 1599
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
800:                     )
801:                     emotion = detect_emotion(
802:                         "media/current_face.jpg"
803:                     )
804: 
805:                     blacklisted_person = check_blacklist(
806:                         "media/current_face.jpg"
807:                     )
808:                     
809:                     print("BLACKLIST RESULT =", blacklisted_person)
810: 
811:                     if blacklisted_person:
812: 
813:                         is_blacklisted = True
814: 
815:                         alert = Alert.objects.create(
816:                             alert_type="Blacklisted Person",
817:                             message=f"{blacklisted_person} detected in {camera_name}"
818:                         )
819: 
820:                         print("ALERT ID =", alert.id)
821: 
822:                         print(f"BLACKLIST DETECTED: {blacklisted_person}")
823: 
824:                         if can_send_email(
825:                             f"blacklist_{blacklisted_person}"
826:                         ):
827: 
828:                             send_alert_email(
829:                                 "🚨 BLACKLISTED PERSON DETECTED",
830:                                 f"{blacklisted_person} detected in {camera_name}"
831:                             )
832:                         filename = datetime.now().strftime(
833:                             "blacklist_%Y%m%d_%H%M%S.jpg"
834:                         )
835: 
836:                         path = os.path.join(
837:                             "media",
838:                             "blacklist_events",
839:                             filename
840:                         )
841: 
842:                         os.makedirs(
843:                             "media/blacklist_events",
844:                             exist_ok=True
845:                         )
846: 
847:                         cv2.imwrite(
848:                             path,
849:                             frame
850:                         )
851:                     # assign or reuse a numeric id for the recognized person
852:                     if recognized_name != "Unknown":
853:                         if recognized_name not in name_id_map:
854:                             name_id_map[recognized_name] = next_person_id
855:                             next_person_id += 1
856:                         current_person_id = name_id_map[recognized_name]
857:                     else:
858:                         # unknowns get a running U<number>
859:                         unknown_counter += 1
860:                         current_person_id = f"U{unknown_counter}"
861: 
862:                     print("Emotion =", emotion)
863: 
864:                     today = date.today()
865: 
866:                     # if recognized_name != "Unknown":
867: 
868:                     #     Alert.objects.create(
869:                     #         alert_type="Known Face",
870:                     #         message=f"{recognized_name} detected in {camera_name}"
871:                     #     )
872: 
873:                     #     if can_send_email(f"known_{recognized_name}"):
874: 
875:                     #         send_alert_email(
876:                     #             "Known Person Detected",
877:                     #             f"{recognized_name} detected in {camera_name}"
878:                     #         )
879: 
880:                     attendance_record = Attendance.objects.filter(
881:                         employee_name=recognized_name,
882:                         date=today
883:                     ).first()
884: 
885:                     if not attendance_record:
886:                         Attendance.objects.create(
887:                             employee_name=recognized_name,
888:                             status="Present"
889:                         )
890:                     else:
891:                         # Update the exit time on subsequent detections
892:                         attendance_record.exit_time = datetime.now().time()
893:                         attendance_record.save()
894: 
895:                     print("Recognized =", recognized_name)
896: 
897:                     now = datetime.now()
898:                     should_save_visitor = False
899:                     visitor_name = recognized_name
900:                     alert_type = None
901:                     alert_message = None
902: 
903:                     if recognized_name == "Unknown":
904:                         alert_type = "Unknown Face"
905:                         alert_message = "Unknown person detected"
906:                         should_save_visitor = (
907:                             last_unknown_time is None or
908:                             now - last_unknown_time > timedelta(seconds=30)
909:                         )
910:                     else:
911:                         if (
912:                             last_recognized_name != recognized_name or
913:                             last_recognized_time is None or
914:                             now - last_recognized_time > timedelta(seconds=30)
915:                         ):
916:                             should_save_visitor = True
917: 
918:                     if should_save_visitor:
919: 
920:                         if alert_type is not None:
921: 
922:                             Alert.objects.create(
923:                                 alert_type="Unknown Face",
924:                                 message=f"Unknown person detected in {camera_name}"
925:                             )
926: 
927:                             print("Unknown Visitor Detected")
928: 
929:                             if can_send_email("unknown_face"):
930: 
931:                                 send_alert_email(
932:                                     "Unknown Face Detected",
933:                                     f"Unknown person detected in {camera_name}"
934:                                 )
935: 
936:                         else:
937: 
938:                             Alert.objects.create(
939:                                 alert_type="Known Face",
940:                                 message=f"{recognized_name} detected in {camera_name}"
941:                             )
942: 
943:                             print(f"Known Visitor Detected: {recognized_name}")
944: 
945:                             if can_send_email(f"known_{recognized_name}"):
946: 
947:                                 send_alert_email(
948:                                     "Known Person Detected",
949:                                     f"{recognized_name} detected in {camera_name}"
950:                                 )
951: 
952:                         filename = now.strftime(
953:                             "%Y%m%d_%H%M%S"
954:                         ) + ".jpg"
955: 
956:                         visitor_folder = os.path.join(
957:                             "media",
958:                             "visitors"
959:                         )
960: 
961:                         os.makedirs(
962:                             visitor_folder,
963:                             exist_ok=True
964:                         )
965: 
966:                         visitor_path = os.path.join(
967:                             visitor_folder,
968:                             filename
969:                         )
970: 
971:                         cv2.imwrite(
972:                             visitor_path,
973:                             frame
974:                         )
975: 
976:                         visitor = Visitorlogo.objects.create(
977:                         visitor_name=visitor_name,
978:                         Snapshot=f"visitors/{filename}",
979:                         camera_name=camera_name
980:                     )
981: 
982:                         print(
983:                             "Visitor Saved:",
984:                             visitor.id
985:                         )
986: 
987:                         if recognized_name == "Unknown":
988:                             last_unknown_time = now
989:                         else:
990:                             last_recognized_time = now
991:                             last_recognized_name = recognized_name
992: 
993:                         
994: 
995:             except Exception as e:
996: 
997:                 print("Recognition Error:", e)
998: 
999:                 recognized_name = "Unknown"
1000:         # ==========================
1001:         # DRAW FACE BOX
1002:         # ==========================
1003:         if is_blacklisted:
1004: 
1005:             cv2.putText(
1006:                 frame,
1007:                 "BLACKLISTED PERSON",
1008:                 (50, 50),
1009:                 cv2.FONT_HERSHEY_SIMPLEX,
1010:                 1,
1011:                 (0, 0, 255),
1012:                 3
1013:             )
1014:         for (x, y, w, h) in faces:
1015: 
1016:             if is_blacklisted:
1017: 
1018:                 box_color = (0, 0, 255)
1019:                 text_color = (0, 0, 255)
1020: 
1021:             elif recognized_name != "Unknown":
1022: 
1023:                 box_color = (0, 255, 0)
1024:                 text_color = (0, 255, 0)
1025: 
1026:             else:
1027: 
1028:                 box_color = (0, 165, 255)
1029:                 text_color = (0, 165, 255)
1030: 
1031: 
1032:             cv2.rectangle(
1033:                 frame,
1034:                 (x, y),
1035:                 (x + w, y + h),
1036:                 box_color,
1037:                 2
1038:             )
1039: 
1040:             # Display name and assigned ID
1041:             id_text = f"ID: {current_person_id}" if current_person_id is not None else "ID: -"
1042:             cv2.putText(
1043:                 frame,
1044:                 f"{recognized_name} | {id_text}",
1045:                 (x, y - 20),
1046:                 cv2.FONT_HERSHEY_SIMPLEX,
1047:                 0.7,
1048:                 text_color,
1049:                 2
1050:             )
1051: 
1052:             # Emotion
1053:             cv2.putText(
1054:                 frame,
1055:                 f"Emotion: {emotion}",
1056:                 (x, y - 5),
1057:                 cv2.FONT_HERSHEY_SIMPLEX,
1058:                 0.6,
1059:                 (255, 255, 0),
1060:                 2
1061:             )
1062: 
1063:         # ==========================
1064:         # STATS
1065:         # ==========================
1066: 
1067:         cv2.putText(
1068:             frame,
1069:             f"Faces: {face_count}",
1070:             (10, 30),
1071:             cv2.FONT_HERSHEY_SIMPLEX,
1072:             0.8,
1073:             (0, 255, 255),
1074:             2
1075:         )
1076: 
1077:         # cv2.putText(
1078:         #     frame,
1079:         #     f"Name: {recognized_name}",
1080:         #     (10, 70),
1081:         #     cv2.FONT_HERSHEY_SIMPLEX,
1082:         #     0.8,
1083:         #     (0, 255, 0),
1084:         #     2
1085:         # )
1086: 
1087:         # cv2.putText(
1088:         #     frame,
1089:         #     f"Emotion: {emotion}",
1090:         #     (10, 150),
1091:         #     cv2.FONT_HERSHEY_SIMPLEX,
1092:         #     0.8,
1093:         #     (255, 0, 255),
1094:         #     2
1095:         # )
1096: 
1097:         cv2.putText(
1098:             frame,
1099:             phone_text,
1100:             (10, 110),
1101:             cv2.FONT_HERSHEY_SIMPLEX,
1102:             0.8,
1103:             (0, 255, 255),
1104:             2
1105:         )
1106: 
1107:         # ==========================
1108:         # JPEG ENCODE
1109:         # ==========================
1110: 
1111:         ret, buffer = cv2.imencode(
1112:             ".jpg",
1113:             frame,
1114:             [cv2.IMWRITE_JPEG_QUALITY, 40]
1115:         )
1116: 
1117:         frame_bytes = buffer.tobytes()
1118: 
1119:         yield (
1120:             b'--frame\r\n'
1121:             b'Content-Type: image/jpeg\r\n\r\n'
1122:             + frame_bytes +
1123:             b'\r\n'
1124:         )
1125:       
1126: 
1127: def video_feed(request, camera_id):
1128:     try:
1129:         camera = Camera.objects.get(id=camera_id)
1130:     except Camera.DoesNotExist:
1131:         return HttpResponse("Camera not found", status=404)
1132:         
1133:     cam_key = f"camera_{camera.id}"
1134:     
1135:     return StreamingHttpResponse(
1136:         generate_frames(camera.id, camera.name, cam_key),
1137:         content_type='multipart/x-mixed-replace; boundary=frame'
1138:     )
1139: 
1140: def occupancy_api(request):
1141:     visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
1142:     return JsonResponse({
1143:         "people": people_count,
1144:         "entry": entry_count,
1145:         "exit": exit_count,
1146:         "occupancy": occupancy,
1147:         "visitors_today": visitors_today
1148:     })
1149: 
1150: 
1151: 
1152: def people_count_api(request):
1153: 
1154:     return JsonResponse({
1155:         "count": people_count
1156:     })
1157: 
1158: def camera_is_active(camera, camera_key):
1159:     try:
1160:         if not camera_enabled.get(camera_key, True):
1161:             return False
1162:         if not camera.isOpened():
1163:             return False
1164: 
1165:         last_seen = camera_last_seen.get(camera_key)
1166:         if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
1167:             return True
1168: 
1169:         # Avoid additional reads from an already-streaming capture device.
1170:         # Use the most recent frame timestamp from the generator to determine activity.
1171:         return False
1172:     except Exception:
1173:         return False
1174: 
1175: 
1176: def camera_status(request):
1177:     global last_camera_status
1178:     global last_camera_status_time
1179:     
1180:     cameras = Camera.objects.filter(is_enabled=True)
1181:     camera_states = {}
1182:     
1183:     for cam in cameras:
1184:         cam_key = f"camera_{cam.id}"
1185:         cam_instance = active_cameras.get(cam_key)
1186:         camera_states[cam_key] = {
1187:             "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
1188:             "name": cam.name
1189:         }
1190: 
1191:     now = datetime.now().strftime("%I:%M %p")
1192:     response_data = {}
1193: 
1194:     for camera_key, data in camera_states.items():
1195:         is_open = data["is_open"]
1196:         readable_name = data["name"]
1197:         
1198:         previous_status = last_camera_status.get(camera_key, False)
1199:         print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
1200: 
1201:         # OFFLINE ALERT
1202:         if previous_status and not is_open:
1203:             print(f"{readable_name} OFFLINE")
1204:             Alert.objects.create(
1205:                 alert_type="Camera Offline",
1206:                 message=f"{readable_name} Offline"
1207:             )
1208:             if can_send_email(f"{camera_key}_offline"):
1209:                 send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
1210:             last_camera_status_time[camera_key] = now
1211: 
1212:         # ONLINE ALERT
1213:         elif not previous_status and is_open:
1214:             print(f"{readable_name} ONLINE")
1215:             Alert.objects.create(
1216:                 alert_type="Camera Online",
1217:                 message=f"{readable_name} Online"
1218:             )
1219:             if can_send_email(f"{camera_key}_online"):
1220:                 send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
1221:             last_camera_status_time[camera_key] = None
1222: 
1223:         last_camera_status[camera_key] = is_open
1224:         
1225:         # Populate response data
1226:         response_data[camera_key] = "Online" if is_open else "Offline"
1227:         response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
1228: 
1229:     return JsonResponse(response_data)
1230:     
1231:     
1232: def control_camera(request):
1233:     camera_id = request.GET.get("camera")
1234:     action = request.GET.get("action", "").lower()
1235:     
1236:     if not camera_id:
1237:         return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
1238:         
1239:     try:
1240:         cam_obj = Camera.objects.get(id=camera_id)
1241:     except Camera.DoesNotExist:
1242:         return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
1243: 
1244:     cam_key = f"camera_{camera_id}"
1245:     
1246:     if action == "start":
1247:         camera_enabled[cam_key] = True
1248:         cam_instance = get_camera_instance(cam_obj.id)
1249:         if cam_instance:
1250:             CameraLog.objects.create(event=f"{cam_obj.name} Started")
1251:             return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
1252:         return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
1253: 
1254:     if action == "stop":
1255:         camera_enabled[cam_key] = False
1256:         if cam_key in active_cameras and active_cameras[cam_key].isOpened():
1257:             active_cameras[cam_key].release()
1258:         CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
1259:         return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
1260: 
1261:     return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
1262: 
1263: 
1264: def capture_snapshot(request):
1265:     camera_id = request.GET.get("camera")
1266:     if not camera_id:
1267:         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
1268:         
1269:     try:
1270:         cam_obj = Camera.objects.get(id=camera_id)
1271:     except Camera.DoesNotExist:
1272:         return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
1273:         
1274:     cam_key = f"camera_{camera_id}"
1275:     camera = active_cameras.get(cam_key)
1276:     
1277:     if camera and camera.isOpened():
1278:         success, frame = camera.read()
1279:         if success:
1280:             filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
1281:             path = os.path.join("media", "snapshots", filename)
1282:             
1283:             # Ensure directory exists
1284:             os.makedirs(os.path.dirname(path), exist_ok=True)
1285:             
1286:             cv2.imwrite(path, frame)
1287:             
1288:             Snapshot.objects.create(
1289:                 image=f"snapshots/{filename}",
1290:                 camera_name=cam_obj.name
1291:             )
1292:             CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
1293:             return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
1294:             
1295:     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
1296: 
1297: def test_log(request):
1298: 
1299:     camera_name = (
1300:         request.POST.get("camera_name") or
1301:         request.GET.get("camera_name") or
1302:         "Unknown Camera"
1303:     )
1304: 
1305:     RecognitionLog.objects.create(
1306:         person_name=recognized_name,
1307:         status="Known",
1308:         camera_name=camera_name
1309:     )
1310: 
1311:     return JsonResponse({
1312:         "message": "saved"
1313:     })
1314: 
1315: 
1316: 
1317: def face_count_api(request):
1318: 
1319:     return JsonResponse({
1320:         "count": face_count
1321:     })
1322: 
1323: 
1324: def alerts_api(request):
1325:     alerts = Alert.objects.order_by('-created_at')[:20]
1326:     total_count = Alert.objects.count()
1327:     last_seen = request.session.get('last_alert_count', 0)
1328:     unread_count = max(0, total_count - last_seen)
1329:     
1330:     alert_list = [
1331:         {
1332:             "alert_type": alert.alert_type,
1333:             "message": alert.message,
1334:             "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
1335:         }
1336:         for alert in alerts
1337:     ]
1338:     return JsonResponse({
1339:         "alerts": alert_list,
1340:         "exit": exit_count,
1341:         "occupancy": occupancy,
1342:         "visitors_today": visitors_today
1343:     })
1344: 
1345: 
1346: 
1347: def people_count_api(request):
1348: 
1349:     return JsonResponse({
1350:         "count": people_count
1351:     })
1352: 
1353: def camera_is_active(camera, camera_key):
1354:     try:
1355:         if not camera_enabled.get(camera_key, True):
1356:             return False
1357:         if not camera.isOpened():
1358:             return False
1359: 
1360:         last_seen = camera_last_seen.get(camera_key)
1361:         if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
1362:             return True
1363: 
1364:         # Avoid additional reads from an already-streaming capture device.
1365:         # Use the most recent frame timestamp from the generator to determine activity.
1366:         return False
1367:     except Exception:
1368:         return False
1369: 
1370: 
1371: def camera_status(request):
1372:     global last_camera_status
1373:     global last_camera_status_time
1374:     
1375:     cameras = Camera.objects.filter(is_enabled=True)
1376:     camera_states = {}
1377:     
1378:     for cam in cameras:
1379:         cam_key = f"camera_{cam.id}"
1380:         cam_instance = active_cameras.get(cam_key)
1381:         camera_states[cam_key] = {
1382:             "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
1383:             "name": cam.name
1384:         }
1385: 
1386:     now = datetime.now().strftime("%I:%M %p")
1387:     response_data = {}
1388: 
1389:     for camera_key, data in camera_states.items():
1390:         is_open = data["is_open"]
1391:         readable_name = data["name"]
1392:         
1393:         previous_status = last_camera_status.get(camera_key, False)
1394:         print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
1395: 
1396:         # OFFLINE ALERT
1397:         if previous_status and not is_open:
1398:             print(f"{readable_name} OFFLINE")
1399:             Alert.objects.create(
1400:                 alert_type="Camera Offline",
1401:                 message=f"{readable_name} Offline"
1402:             )
1403:             if can_send_email(f"{camera_key}_offline"):
1404:                 send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
1405:             last_camera_status_time[camera_key] = now
1406: 
1407:         # ONLINE ALERT
1408:         elif not previous_status and is_open:
1409:             print(f"{readable_name} ONLINE")
1410:             Alert.objects.create(
1411:                 alert_type="Camera Online",
1412:                 message=f"{readable_name} Online"
1413:             )
1414:             if can_send_email(f"{camera_key}_online"):
1415:                 send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
1416:             last_camera_status_time[camera_key] = None
1417: 
1418:         last_camera_status[camera_key] = is_open
1419:         
1420:         # Populate response data
1421:         response_data[camera_key] = "Online" if is_open else "Offline"
1422:         response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
1423: 
1424:     return JsonResponse(response_data)
1425:     
1426:     
1427: def control_camera(request):
1428:     camera_id = request.GET.get("camera")
1429:     action = request.GET.get("action", "").lower()
1430:     
1431:     if not camera_id:
1432:         return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
1433:         
1434:     try:
1435:         cam_obj = Camera.objects.get(id=camera_id)
1436:     except Camera.DoesNotExist:
1437:         return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
1438: 
1439:     cam_key = f"camera_{camera_id}"
1440:     
1441:     if action == "start":
1442:         camera_enabled[cam_key] = True
1443:         cam_instance = get_camera_instance(cam_obj.id)
1444:         if cam_instance:
1445:             CameraLog.objects.create(event=f"{cam_obj.name} Started")
1446:             return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
1447:         return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
1448: 
1449:     if action == "stop":
1450:         camera_enabled[cam_key] = False
1451:         if cam_key in active_cameras and active_cameras[cam_key].isOpened():
1452:             active_cameras[cam_key].release()
1453:         CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
1454:         return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
1455: 
1456:     return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
1457: 
1458: 
1459: def capture_snapshot(request):
1460:     camera_id = request.GET.get("camera")
1461:     if not camera_id:
1462:         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
1463:         
1464:     try:
1465:         cam_obj = Camera.objects.get(id=camera_id)
1466:     except Camera.DoesNotExist:
1467:         return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
1468:         
1469:     cam_key = f"camera_{camera_id}"
1470:     camera = active_cameras.get(cam_key)
1471:     
1472:     if camera and camera.isOpened():
1473:         success, frame = camera.read()
1474:         if success:
1475:             filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
1476:             path = os.path.join("media", "snapshots", filename)
1477:             
1478:             # Ensure directory exists
1479:             os.makedirs(os.path.dirname(path), exist_ok=True)
1480:             
1481:             cv2.imwrite(path, frame)
1482:             
1483:             Snapshot.objects.create(
1484:                 image=f"snapshots/{filename}",
1485:                 camera_name=cam_obj.name
1486:             )
1487:             CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
1488:             return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
1489:             
1490:     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
1491: 
1492: def test_log(request):
1493: 
1494:     camera_name = (
1495:         request.POST.get("camera_name") or
1496:         request.GET.get("camera_name") or
1497:         "Unknown Camera"
1498:     )
1499: 
1500:     RecognitionLog.objects.create(
1501:         person_name=recognized_name,
1502:         status="Known",
1503:         camera_name=camera_name
1504:     )
1505: 
1506:     return JsonResponse({
1507:         "message": "saved"
1508:     })
1509: 
1510: 
1511: 
1512: def face_count_api(request):
1513: 
1514:     return JsonResponse({
1515:         "count": face_count
1516:     })
1517: 
1518: 
1519: def alerts_api(request):
1520:     alerts = Alert.objects.order_by('-created_at')[:20]
1521:     total_count = Alert.objects.count()
1522:     last_seen = request.session.get('last_alert_count', 0)
1523:     unread_count = max(0, total_count - last_seen)
1524:     
1525:     alert_list = [
1526:         {
1527:             "alert_type": alert.alert_type,
1528:             "message": alert.message,
1529:             "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
1530:         }
1531:         for alert in alerts
1532:     ]
1533:     return JsonResponse({
1534:         "alerts": alert_list,
1535:         "total_count": total_count,
1536:         "unread_count": unread_count
1537:     })   
1538:     
1539: def mark_alerts_read(request):
1540:     if request.method == "POST":
1541:         request.session['last_alert_count'] = Alert.objects.count()
1542:         return JsonResponse({"status": "success"})
1543:     return JsonResponse({"status": "error"}, status=400)
1544: 
1545: @login_required
1546: @require_http_methods(["POST"])
1547: @csrf_exempt
1548: def delete_alerts(request):
1549:     try:
1550:         import json
1551:         data = json.loads(request.body)
1552:         alert_ids = [int(i) for i in data.get('alert_ids', []) if str(i).isdigit()]
1553:         if not alert_ids:
1554:             return JsonResponse({"status": "error", "message": "No alert IDs provided"}, status=400)
1555:         
1556:         # Delete selected alerts
1557:         deleted_count, _ = Alert.objects.filter(id__in=alert_ids).delete()
1558:         return JsonResponse({"status": "success", "deleted_count": deleted_count})
1559:     except Exception as e:
1560:         return JsonResponse({"status": "error", "message": str(e)}, status=500)
1561: 
1562: @login_required
1563: @require_http_methods(["POST"])
1564: @csrf_exempt
1565: def delete_logs(request):
1566:     try:
1567:         import json
1568:         data = json.loads(request.body)
1569:         log_ids = [int(i) for i in data.get('log_ids', []) if str(i).isdigit()]
1570:         if not log_ids:
1571:             return JsonResponse({"status": "error", "message": "No log IDs provided"}, status=400)
1572:         
1573:         # Delete selected logs
1574:         deleted_count, _ = CameraLog.objects.filter(id__in=log_ids).delete()
1575:         return JsonResponse({"status": "success", "deleted_count": deleted_count})
1576:     except Exception as e:
1577:         return JsonResponse({"status": "error", "message": str(e)}, status=500)
1578: 
1579: @login_required
1580: def download_report(request):
1581:     from datetime import datetime
1582:     
1583:     start_date_str = request.GET.get('start_date')
1584:     end_date_str = request.GET.get('end_date')
1585:     
1586:     rec_filter, alert_filter, snap_filter, log_filter, att_filter = {}, {}, {}, {}, {}
1587:     date_range_str = "All Time"
1588:     
1589:     if start_date_str and end_date_str:
1590:         rec_filter['detection_time__range'] = [f"{start_date_str} 00:00:00", f"{end_date_str} 23:59:59"]
1591:         alert_filter['created_at__range'] = [f"{start_date_str} 00:00:00", f"{end_date_str} 23:59:59"]
1592:         snap_filter['created_at__range'] = [f"{start_date_str} 00:00:00", f"{end_date_str} 23:59:59"]
1593:         log_filter['created_at__range'] = [f"{start_date_str} 00:00:00", f"{end_date_str} 23:59:59"]
1594:         att_filter['date__range'] = [start_date_str, end_date_str]
1595:         date_range_str = f"{start_date_str} to {end_date_str}"
1596: 
1597:     response = HttpResponse(content_type='application/pdf')
1598:     response['Content-Disposition'] = 'attachment; filename="Smart_CCTV_Report.pdf"'
1599: 
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.

--- MATCH 1 ---
Created At: 2026-06-29T10:30:35Z
Completed At: 2026-06-29T10:30:36Z
File Path: `file:///c:/private/Project/SmartCCTV/dashboard/views.py`
Total Lines: 1808
Total Bytes: 61288
Showing lines 1370 to 1580
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1370:     return JsonResponse({
1371:         "alerts": alert_list,
1372:         "exit": exit_count,
1373:         "occupancy": occupancy,
1374:         "visitors_today": visitors_today
1375:     })
1376: 
1377: 
1378: 
1379: def people_count_api(request):
1380: 
1381:     return JsonResponse({
1382:         "count": people_count
1383:     })
1384: 
1385: def camera_is_active(camera, camera_key):
1386:     try:
1387:         if not camera_enabled.get(camera_key, True):
1388:             return False
1389:         if not camera.isOpened():
1390:             return False
1391: 
1392:         last_seen = camera_last_seen.get(camera_key)
1393:         if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
1394:             return True
1395: 
1396:         # Avoid additional reads from an already-streaming capture device.
1397:         # Use the most recent frame timestamp from the generator to determine activity.
1398:         return False
1399:     except Exception:
1400:         return False
1401: 
1402: 
1403: def camera_status(request):
1404:     global last_camera_status
1405:     global last_camera_status_time
1406:     
1407:     cameras = Camera.objects.filter(is_enabled=True)
1408:     camera_states = {}
1409:     
1410:     for cam in cameras:
1411:         cam_key = f"camera_{cam.id}"
1412:         cam_instance = active_cameras.get(cam_key)
1413:         camera_states[cam_key] = {
1414:             "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
1415:             "name": cam.name
1416:         }
1417: 
1418:     now = datetime.now().strftime("%I:%M %p")
1419:     response_data = {}
1420: 
1421:     for camera_key, data in camera_states.items():
1422:         is_open = data["is_open"]
1423:         readable_name = data["name"]
1424:         
1425:         previous_status = last_camera_status.get(camera_key, False)
1426:         print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
1427: 
1428:         # OFFLINE ALERT
1429:         if previous_status and not is_open:
1430:             print(f"{readable_name} OFFLINE")
1431:             Alert.objects.create(
1432:                 alert_type="Camera Offline",
1433:                 message=f"{readable_name} Offline"
1434:             )
1435:             if can_send_email(f"{camera_key}_offline"):
1436:                 send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
1437:             last_camera_status_time[camera_key] = now
1438: 
1439:         # ONLINE ALERT
1440:         elif not previous_status and is_open:
1441:             print(f"{readable_name} ONLINE")
1442:             Alert.objects.create(
1443:                 alert_type="Camera Online",
1444:                 message=f"{readable_name} Online"
1445:             )
1446:             if can_send_email(f"{camera_key}_online"):
1447:                 send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
1448:             last_camera_status_time[camera_key] = None
1449: 
1450:         last_camera_status[camera_key] = is_open
1451:         
1452:         # Populate response data
1453:         response_data[camera_key] = "Online" if is_open else "Offline"
1454:         response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
1455: 
1456:     return JsonResponse(response_data)
1457:     
1458:     
1459: def control_camera(request):
1460:     camera_id = request.GET.get("camera")
1461:     action = request.GET.get("action", "").lower()
1462:     
1463:     if not camera_id:
1464:         return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
1465:         
1466:     try:
1467:         cam_obj = Camera.objects.get(id=camera_id)
1468:     except Camera.DoesNotExist:
1469:         return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
1470: 
1471:     cam_key = f"camera_{camera_id}"
1472:     
1473:     if action == "start":
1474:         camera_enabled[cam_key] = True
1475:         cam_instance = get_camera_instance(cam_obj.id)
1476:         if cam_instance:
1477:             CameraLog.objects.create(event=f"{cam_obj.name} Started")
1478:             return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
1479:         return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
1480: 
1481:     if action == "stop":
1482:         camera_enabled[cam_key] = False
1483:         if cam_key in active_cameras and active_cameras[cam_key].isOpened():
1484:             active_cameras[cam_key].release()
1485:         CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
1486:         return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
1487: 
1488:     return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
1489: 
1490: 
1491: def capture_snapshot(request):
1492:     camera_id = request.GET.get("camera")
1493:     if not camera_id:
1494:         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
1495:         
1496:     try:
1497:         cam_obj = Camera.objects.get(id=camera_id)
1498:     except Camera.DoesNotExist:
1499:         return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
1500:         
1501:     cam_key = f"camera_{camera_id}"
1502:     camera = active_cameras.get(cam_key)
1503:     
1504:     if camera and camera.isOpened():
1505:         success, frame = camera.read()
1506:         if success:
1507:             filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
1508:             path = os.path.join("media", "snapshots", filename)
1509:             
1510:             # Ensure directory exists
1511:             os.makedirs(os.path.dirname(path), exist_ok=True)
1512:             
1513:             cv2.imwrite(path, frame)
1514:             
1515:             Snapshot.objects.create(
1516:                 image=f"snapshots/{filename}",
1517:                 camera_name=cam_obj.name
1518:             )
1519:             CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
1520:             return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
1521:             
1522:     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
1523: 
1524: def test_log(request):
1525: 
1526:     camera_name = (
1527:         request.POST.get("camera_name") or
1528:         request.GET.get("camera_name") or
1529:         "Unknown Camera"
1530:     )
1531: 
1532:     RecognitionLog.objects.create(
1533:         person_name=recognized_name,
1534:         status="Known",
1535:         camera_name=camera_name
1536:     )
1537: 
1538:     return JsonResponse({
1539:         "message": "saved"
1540:     })
1541: 
1542: 
1543: 
1544: def face_count_api(request):
1545: 
1546:     return JsonResponse({
1547:         "count": face_count
1548:     })
1549: 
1550: 
1551: def alerts_api(request):
1552:     alerts = Alert.objects.order_by('-created_at')[:20]
1553:     total_count = Alert.objects.count()
1554:     last_seen = request.session.get('last_alert_count', 0)
1555:     unread_count = max(0, total_count - last_seen)
1556:     
1557:     alert_list = [
1558:         {
1559:             "alert_type": alert.alert_type,
1560:             "message": alert.message,
1561:             "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
1562:         }
1563:         for alert in alerts
1564:     ]
1565:     return JsonResponse({
1566:         "alerts": alert_list,
1567:         "total_count": total_count,
1568:         "unread_count": unread_count
1569:     })   
1570:     
1571: def mark_alerts_read(request):
1572:     if request.method == "POST":
1573:         request.session['last_alert_count'] = Alert.objects.count()
1574:         return JsonResponse({"status": "success"})
1575:     return JsonResponse({"status": "error"}, status=400)
1576: 
1577: @login_required
1578: @require_http_methods(["POST"])
1579: @csrf_exempt
1580: def delete_alerts(request):
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.

--- MATCH 2 ---
Created At: 2026-06-29T10:31:32Z
Completed At: 2026-06-29T10:31:33Z
File Path: `file:///c:/private/Project/SmartCCTV/dashboard/views.py`
Total Lines: 1809
Total Bytes: 61349
Showing lines 1340 to 1600
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1340:         camera_name=camera_name
1341:     )
1342: 
1343:     return JsonResponse({
1344:         "message": "saved"
1345:     })
1346: 
1347: 
1348: 
1349:             + frame_bytes +
1350:             b'\r\n'
1351:         )
1352:       
1353: 
1354: def video_feed(request, camera_id):
1355:     try:
1356:         camera = Camera.objects.get(id=camera_id)
1357:     except Camera.DoesNotExist:
1358:         return HttpResponse("Camera not found", status=404)
1359:         
1360:     cam_key = f"camera_{camera.id}"
1361:     
1362:     return StreamingHttpResponse(
1363:         generate_frames(camera.id, camera.name, cam_key),
1364:         content_type='multipart/x-mixed-replace; boundary=frame'
1365:     )
1366: 
1367: def occupancy_api(request):
1368:     visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
1369:     return JsonResponse({
1370:         "people": people_count,
1371:         "entry": entry_count,
1372:         "exit": exit_count,
1373:         "occupancy": occupancy,
1374:         "visitors_today": visitors_today
1375:     })
1376: 
1377: 
1378: 
1379: def people_count_api(request):
1380: 
1381:     return JsonResponse({
1382:         "count": people_count
1383:     })
1384: 
1385: def camera_is_active(camera, camera_key):
1386:     try:
1387:         if not camera_enabled.get(camera_key, True):
1388:             return False
1389:         if not camera.isOpened():
1390:             return False
1391: 
1392:         last_seen = camera_last_seen.get(camera_key)
1393:         if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
1394:             return True
1395: 
1396:         # Avoid additional reads from an already-streaming capture device.
1397:         # Use the most recent frame timestamp from the generator to determine activity.
1398:         return False
1399:     except Exception:
1400:         return False
1401: 
1402: 
1403: def camera_status(request):
1404:     global last_camera_status
1405:     global last_camera_status_time
1406:     
1407:     cameras = Camera.objects.filter(is_enabled=True)
1408:     camera_states = {}
1409:     
1410:     for cam in cameras:
1411:         cam_key = f"camera_{cam.id}"
1412:         cam_instance = active_cameras.get(cam_key)
1413:         camera_states[cam_key] = {
1414:             "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
1415:             "name": cam.name
1416:         }
1417: 
1418:     now = datetime.now().strftime("%I:%M %p")
1419:     response_data = {}
1420: 
1421:     for camera_key, data in camera_states.items():
1422:         is_open = data["is_open"]
1423:         readable_name = data["name"]
1424:         
1425:         previous_status = last_camera_status.get(camera_key, False)
1426:         print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
1427: 
1428:         # OFFLINE ALERT
1429:         if previous_status and not is_open:
1430:             print(f"{readable_name} OFFLINE")
1431:             Alert.objects.create(
1432:                 alert_type="Camera Offline",
1433:                 message=f"{readable_name} Offline"
1434:             )
1435:             if can_send_email(f"{camera_key}_offline"):
1436:                 send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
1437:             last_camera_status_time[camera_key] = now
1438: 
1439:         # ONLINE ALERT
1440:         elif not previous_status and is_open:
1441:             print(f"{readable_name} ONLINE")
1442:             Alert.objects.create(
1443:                 alert_type="Camera Online",
1444:                 message=f"{readable_name} Online"
1445:             )
1446:             if can_send_email(f"{camera_key}_online"):
1447:                 send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
1448:             last_camera_status_time[camera_key] = None
1449: 
1450:         last_camera_status[camera_key] = is_open
1451:         
1452:         # Populate response data
1453:         response_data[camera_key] = "Online" if is_open else "Offline"
1454:         response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
1455: 
1456:     return JsonResponse(response_data)
1457:     
1458:     
1459: def control_camera(request):
1460:     camera_id = request.GET.get("camera")
1461:     action = request.GET.get("action", "").lower()
1462:     
1463:     if not camera_id:
1464:         return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
1465:         
1466:     try:
1467:         cam_obj = Camera.objects.get(id=camera_id)
1468:     except Camera.DoesNotExist:
1469:         return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
1470: 
1471:     cam_key = f"camera_{camera_id}"
1472:     
1473:     if action == "start":
1474:         camera_enabled[cam_key] = True
1475:         cam_instance = get_camera_instance(cam_obj.id)
1476:         if cam_instance:
1477:             CameraLog.objects.create(event=f"{cam_obj.name} Started")
1478:             return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
1479:         return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
1480: 
1481:     if action == "stop":
1482:         camera_enabled[cam_key] = False
1483:         if cam_key in active_cameras and active_cameras[cam_key].isOpened():
1484:             active_cameras[cam_key].release()
1485:         CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
1486:         return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
1487: 
1488:     return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
1489: 
1490: 
1491: def capture_snapshot(request):
1492:     camera_id = request.GET.get("camera")
1493:     if not camera_id:
1494:         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
1495:         
1496:     try:
1497:         cam_obj = Camera.objects.get(id=camera_id)
1498:     except Camera.DoesNotExist:
1499:         return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
1500:         
1501:     cam_key = f"camera_{camera_id}"
1502:     camera = active_cameras.get(cam_key)
1503:     
1504:     if camera and camera.isOpened():
1505:         success, frame = camera.read()
1506:         if success:
1507:             filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
1508:             path = os.path.join("media", "snapshots", filename)
1509:             
1510:             # Ensure directory exists
1511:             os.makedirs(os.path.dirname(path), exist_ok=True)
1512:             
1513:             cv2.imwrite(path, frame)
1514:             
1515:             Snapshot.objects.create(
1516:                 image=f"snapshots/{filename}",
1517:                 camera_name=cam_obj.name
1518:             )
1519:             CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
1520:             return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
1521:             
1522:     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
1523: 
1524: def test_log(request):
1525: 
1526:     camera_name = (
1527:         request.POST.get("camera_name") or
1528:         request.GET.get("camera_name") or
1529:         "Unknown Camera"
1530:     )
1531: 
1532:     RecognitionLog.objects.create(
1533:         person_name=recognized_name,
1534:         status="Known",
1535:         camera_name=camera_name
1536:     )
1537: 
1538:     return JsonResponse({
1539:         "message": "saved"
1540:     })
1541: 
1542: 
1543: 
1544: def face_count_api(request):
1545: 
1546:     return JsonResponse({
1547:         "count": face_count
1548:     })
1549: 
1550: 
1551: def alerts_api(request):
1552:     alerts = Alert.objects.order_by('-created_at')[:20]
1553:     total_count = Alert.objects.count()
1554:     last_seen = request.session.get('last_alert_count', 0)
1555:     unread_count = max(0, total_count - last_seen)
1556:     
1557:     alert_list = [
1558:         {
1559:             "alert_type": alert.alert_type,
1560:             "message": alert.message,
1561:             "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
1562:         }
1563:         for alert in alerts
1564:     ]
1565:     return JsonResponse({
1566:         "alerts": alert_list,
1567:         "exit": exit_count,
1568:         "occupancy": occupancy,
1569:         "visitors_today": visitors_today
1570:     })
1571:     
1572: def mark_alerts_read(request):
1573:     if request.method == "POST":
1574:         request.session['last_alert_count'] = Alert.objects.count()
1575:         return JsonResponse({"status": "success"})
1576:     return JsonResponse({"status": "error"}, status=400)
1577: 
1578: @login_required
1579: @require_http_methods(["POST"])
1580: @csrf_exempt
1581: def delete_alerts(request):
1582:     try:
1583:         import json
1584:         data = json.loads(request.body)
1585:         alert_ids = [int(i) for i in data.get('alert_ids', []) if str(i).isdigit()]
1586:         if not alert_ids:
1587:             return JsonResponse({"status": "error", "message": "No alert IDs provided"}, status=400)
1588:         
1589:         # Delete selected alerts
1590:         deleted_count, _ = Alert.objects.filter(id__in=alert_ids).delete()
1591:         return JsonResponse({"status": "success", "deleted_count": deleted_count})
1592:     except Exception as e:
1593:         return JsonResponse({"status": "error", "message": str(e)}, status=500)
1594: 
1595: @login_required
1596: @require_http_methods(["POST"])
1597: @csrf_exempt
1598: def delete_logs(request):
1599:     try:
1600:         import json
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.

--- MATCH 3 ---
Created At: 2026-06-29T10:32:21Z
Completed At: 2026-06-29T10:32:23Z
The following changes were made by the replace_file_content tool to: c:\private\Project\SmartCCTV\dashboard\views.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -1343,233 +1343,9 @@
     return JsonResponse({
         "message": "saved"
     })
-
-
-
-            + frame_bytes +
-            b'\r\n'
-        )
-      
-
-def video_feed(request, camera_id):
-    try:
-        camera = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return HttpResponse("Camera not found", status=404)
-        
-    cam_key = f"camera_{camera.id}"
-    
-    return StreamingHttpResponse(
-        generate_frames(camera.id, camera.name, cam_key),
-        content_type='multipart/x-mixed-replace; boundary=frame'
-    )
-
-def occupancy_api(request):
-    visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
-    return JsonResponse({
-        "people": people_count,
-        "entry": entry_count,
-        "exit": exit_count,
-        "occupancy": occupancy,
-        "visitors_today": visitors_today
-    })
-
-
-
-def people_count_api(request):
-
-    return JsonResponse({
-        "count": people_count
-    })
-
-def camera_is_active(camera, camera_key):
-    try:
-        if not camera_enabled.get(camera_key, True):
-            return False
-        if not camera.isOpened():
-            return False
-
-        last_seen = camera_last_seen.get(camera_key)
-        if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
-            return True
-
-        # Avoid additional reads from an already-streaming capture device.
-        # Use the most recent frame timestamp from the generator to determine activity.
-        return False
-    except Exception:
-        return False
-
-
-def camera_status(request):
-    global last_camera_status
-    global last_camera_status_time
-    
-    cameras = Camera.objects.filter(is_enabled=True)
-    camera_states = {}
-    
-    for cam in cameras:
-        cam_key = f"camera_{cam.id}"
-        cam_instance = active_cameras.get(cam_key)
-        camera_states[cam_key] = {
-            "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
-            "name": cam.name
-        }
-
-    now = datetime.now().strftime("%I:%M %p")
-    response_data = {}
-
-    for camera_key, data in camera_states.items():
-        is_open = data["is_open"]
-        readable_name = data["name"]
-        
-        previous_status = last_camera_status.get(camera_key, False)
-        print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
-
-        # OFFLINE ALERT
-        if previous_status and not is_open:
-            print(f"{readable_name} OFFLINE")
-            Alert.objects.create(
-                alert_type="Camera Offline",
-                message=f"{readable_name} Offline"
-            )
-            if can_send_email(f"{camera_key}_offline"):
-                send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
-            last_camera_status_time[camera_key] = now
-
-        # ONLINE ALERT
-        elif not previous_status and is_open:
-            print(f"{readable_name} ONLINE")
-            Alert.objects.create(
-                alert_type="Camera Online",
-                message=f"{readable_name} Online"
-            )
-            if can_send_email(f"{camera_key}_online"):
-                send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
-            last_camera_status_time[camera_key] = None
-
-        last_camera_status[camera_key] = is_open
-        
-        # Populate response data
-        response_data[camera_key] = "Online" if is_open else "Offline"
-        response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
-
-    return JsonResponse(response_data)
-    
-    
-def control_camera(request):
-    camera_id = request.GET.get("camera")
-    action = request.GET.get("action", "").lower()
-    
-    if not camera_id:
-        return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
-        
-    try:
-        cam_obj = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
-
-    cam_key = f"camera_{camera_id}"
-    
-    if action == "start":
-        camera_enabled[cam_key] = True
-        cam_instance = get_camera_instance(cam_obj.id)
-        if cam_instance:
-            CameraLog.objects.create(event=f"{cam_obj.name} Started")
-            return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
-        return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
-
-    if action == "stop":
-        camera_enabled[cam_key] = False
-        if cam_key in active_cameras and active_cameras[cam_key].isOpened():
-            active_cameras[cam_key].release()
-        CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
-        return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
-
-    return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
-
-
-def capture_snapshot(request):
-    camera_id = request.GET.get("camera")
-    if not camera_id:
-        return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
-        
-    try:
-        cam_obj = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
-        
-    cam_key = f"camera_{camera_id}"
-    camera = active_cameras.get(cam_key)
-    
-    if camera and camera.isOpened():
-        success, frame = camera.read()
-        if success:
-            filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
-            path = os.path.join("media", "snapshots", filename)
-            
-            # Ensure directory exists
-            os.makedirs(os.path.dirname(path), exist_ok=True)
-            
-            cv2.imwrite(path, frame)
-            
-            Snapshot.objects.create(
-                image=f"snapshots/{filename}",
-                camera_name=cam_obj.name
-            )
-            CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
-            return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
-            
-    return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
-
-def test_log(request):
-
-    camera_name = (
-        request.POST.get("camera_name") or
-        request.GET.get("camera_name") or
-        "Unknown Camera"
-    )
-
-    RecognitionLog.objects.create(
-        person_name=recognized_name,
-        status="Known",
-        camera_name=camera_name
-    )
-
-    return JsonResponse({
-        "message": "saved"
-    })
-
-
-
-def face_count_api(request):
-
-    return JsonResponse({
-        "count": face_count
-    })
-
-
-def alerts_api(request):
-    alerts = Alert.objects.order_by('-created_at')[:20]
-    total_count = Alert.objects.count()
-    last_seen = request.session.get('last_alert_count', 0)
-    unread_count = max(0, total_count - last_seen)
-    
-    alert_list = [
-        {
-            "alert_type": alert.alert_type,
-            "message": alert.message,
-            "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
-        }
-        for alert in alerts
-    ]
-    return JsonResponse({
-        "alerts": alert_list,
-        "exit": exit_count,
-        "occupancy": occupancy,
-        "visitors_today": visitors_today
-    })
-    
-def mark_alerts_read(request):
+
+
+est):
     if request.method == "POST":
         request.session['last_alert_count'] = Alert.objects.count()
         return JsonResponse({"status": "success"})
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

You had inaccuracies in your replacement chunks, so you should review the file contents before making further edits. It is important that you get the target content EXACTLY right, otherwise you will make more mistakes.
--- MATCH 4 ---
Created At: 2026-06-29T10:35:55Z
Completed At: 2026-06-29T10:35:55Z
File Path: `file:///c:/private/Project/SmartCCTV/dashboard/views.py`
Total Lines: 1584
Total Bytes: 54133
Showing lines 1155 to 1350
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1155:             b'\r\n'
1156:         )
1157:       
1158: 
1159: def video_feed(request, camera_id):
1160:     try:
1161:         camera = Camera.objects.get(id=camera_id)
1162:     except Camera.DoesNotExist:
1163:         return HttpResponse("Camera not found", status=404)
1164:         
1165:     cam_key = f"camera_{camera.id}"
1166:     
1167:     return StreamingHttpResponse(
1168:         generate_frames(camera.id, camera.name, cam_key),
1169:         content_type='multipart/x-mixed-replace; boundary=frame'
1170:     )
1171: 
1172: def occupancy_api(request):
1173:     visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
1174:     return JsonResponse({
1175:         "people": people_count,
1176:         "entry": entry_count,
1177:         "exit": exit_count,
1178:         "occupancy": occupancy,
1179:         "visitors_today": visitors_today
1180:     })
1181: 
1182: 
1183: 
1184: def people_count_api(request):
1185: 
1186:     return JsonResponse({
1187:         "count": people_count
1188:     })
1189: 
1190: def camera_is_active(camera, camera_key):
1191:     try:
1192:         if not camera_enabled.get(camera_key, True):
1193:             return False
1194:         if not camera.isOpened():
1195:             return False
1196: 
1197:         last_seen = camera_last_seen.get(camera_key)
1198:         if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
1199:             return True
1200: 
1201:         # Avoid additional reads from an already-streaming capture device.
1202:         # Use the most recent frame timestamp from the generator to determine activity.
1203:         return False
1204:     except Exception:
1205:         return False
1206: 
1207: 
1208: def camera_status(request):
1209:     global last_camera_status
1210:     global last_camera_status_time
1211:     
1212:     cameras = Camera.objects.filter(is_enabled=True)
1213:     camera_states = {}
1214:     
1215:     for cam in cameras:
1216:         cam_key = f"camera_{cam.id}"
1217:         cam_instance = active_cameras.get(cam_key)
1218:         camera_states[cam_key] = {
1219:             "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
1220:             "name": cam.name
1221:         }
1222: 
1223:     now = datetime.now().strftime("%I:%M %p")
1224:     response_data = {}
1225: 
1226:     for camera_key, data in camera_states.items():
1227:         is_open = data["is_open"]
1228:         readable_name = data["name"]
1229:         
1230:         previous_status = last_camera_status.get(camera_key, False)
1231:         print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
1232: 
1233:         # OFFLINE ALERT
1234:         if previous_status and not is_open:
1235:             print(f"{readable_name} OFFLINE")
1236:             Alert.objects.create(
1237:                 alert_type="Camera Offline",
1238:                 message=f"{readable_name} Offline"
1239:             )
1240:             if can_send_email(f"{camera_key}_offline"):
1241:                 send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
1242:             last_camera_status_time[camera_key] = now
1243: 
1244:         # ONLINE ALERT
1245:         elif not previous_status and is_open:
1246:             print(f"{readable_name} ONLINE")
1247:             Alert.objects.create(
1248:                 alert_type="Camera Online",
1249:                 message=f"{readable_name} Online"
1250:             )
1251:             if can_send_email(f"{camera_key}_online"):
1252:                 send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
1253:             last_camera_status_time[camera_key] = None
1254: 
1255:         last_camera_status[camera_key] = is_open
1256:         
1257:         # Populate response data
1258:         response_data[camera_key] = "Online" if is_open else "Offline"
1259:         response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
1260: 
1261:     return JsonResponse(response_data)
1262:     
1263:     
1264: def control_camera(request):
1265:     camera_id = request.GET.get("camera")
1266:     action = request.GET.get("action", "").lower()
1267:     
1268:     if not camera_id:
1269:         return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
1270:         
1271:     try:
1272:         cam_obj = Camera.objects.get(id=camera_id)
1273:     except Camera.DoesNotExist:
1274:         return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
1275: 
1276:     cam_key = f"camera_{camera_id}"
1277:     
1278:     if action == "start":
1279:         camera_enabled[cam_key] = True
1280:         cam_instance = get_camera_instance(cam_obj.id)
1281:         if cam_instance:
1282:             CameraLog.objects.create(event=f"{cam_obj.name} Started")
1283:             return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
1284:         return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
1285: 
1286:     if action == "stop":
1287:         camera_enabled[cam_key] = False
1288:         if cam_key in active_cameras and active_cameras[cam_key].isOpened():
1289:             active_cameras[cam_key].release()
1290:         CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
1291:         return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
1292: 
1293:     return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
1294: 
1295: 
1296: def capture_snapshot(request):
1297:     camera_id = request.GET.get("camera")
1298:     if not camera_id:
1299:         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
1300:         
1301:     try:
1302:         cam_obj = Camera.objects.get(id=camera_id)
1303:     except Camera.DoesNotExist:
1304:         return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
1305:         
1306:     cam_key = f"camera_{camera_id}"
1307:     camera = active_cameras.get(cam_key)
1308:     
1309:     if camera and camera.isOpened():
1310:         success, frame = camera.read()
1311:         if success:
1312:             filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
1313:             path = os.path.join("media", "snapshots", filename)
1314:             
1315:             # Ensure directory exists
1316:             os.makedirs(os.path.dirname(path), exist_ok=True)
1317:             
1318:             cv2.imwrite(path, frame)
1319:             
1320:             Snapshot.objects.create(
1321:                 image=f"snapshots/{filename}",
1322:                 camera_name=cam_obj.name
1323:             )
1324:             CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
1325:             return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
1326:             
1327:     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
1328: 
1329: def test_log(request):
1330: 
1331:     camera_name = (
1332:         request.POST.get("camera_name") or
1333:         request.GET.get("camera_name") or
1334:         "Unknown Camera"
1335:     )
1336: 
1337:     RecognitionLog.objects.create(
1338:         person_name=recognized_name,
1339:         status="Known",
1340:         camera_name=camera_name
1341:     )
1342: 
1343:     return JsonResponse({
1344:         "message": "saved"
1345:     })
1346: 
1347: def mark_alerts_read(request):
1348:     if request.method == "POST":
1349:         request.session['last_alert_count'] = Alert.objects.count()
1350:         return JsonResponse({"status": "success"})
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.

--- MATCH 5 ---
Created At: 2026-06-29T10:36:07Z
Completed At: 2026-06-29T10:36:21Z
The following changes were made by the replace_file_content tool to: c:\private\Project\SmartCCTV\dashboard\views.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -1335,14 +1335,234 @@
     )
 
     RecognitionLog.objects.create(
-        person_name=recognized_name,
-        status="Known",
-        camera_name=camera_name
-    )
-
-    return JsonResponse({
-        "message": "saved"
-    })
+        ret, buffer = cv2.imencode(
+            ".jpg",
+            frame,
+            [cv2.IMWRITE_JPEG_QUALITY, 40]
+        )
+
+        frame_bytes = buffer.tobytes()
+
+        yield (
+            b'--frame\r\n'
+            b'Content-Type: image/jpeg\r\n\r\n'
+            + frame_bytes +
+            b'\r\n'
+        )
+      
+
+def video_feed(request, camera_id):
+    try:
+        camera = Camera.objects.get(id=camera_id)
+    except Camera.DoesNotExist:
+        return HttpResponse("Camera not found", status=404)
+        
+    cam_key = f"camera_{camera.id}"
+    
+    return StreamingHttpResponse(
+        generate_frames(camera.id, camera.name, cam_key),
+        content_type='multipart/x-mixed-replace; boundary=frame'
+    )
+
+def occupancy_api(request):
+    visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
+    return JsonResponse({
+        "people": people_count,
+        "entry": entry_count,
+        "exit": exit_count,
+        "occupancy": occupancy,
+        "visitors_today": visitors_today
+    })
+
+
+
+def people_count_api(request):
+
+    return JsonResponse({
+        "count": people_count
+    })
+
+def camera_is_active(camera, camera_key):
+    try:
+        if not camera_enabled.get(camera_key, True):
+            return False
+        if not camera.isOpened():
+            return False
+
+        last_seen = camera_last_seen.get(camera_key)
+        if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
+            return True
+
+        # Avoid additional reads from an already-streaming capture device.
+        # Use the most recent frame timestamp from the generator to determine activity.
+        return False
+    except Exception:
+        return False
+
+
+def camera_status(request):
+    global last_camera_status
+    global last_camera_status_time
+    
+    cameras = Camera.objects.filter(is_enabled=True)
+    camera_states = {}
+    
+    for cam in cameras:
+        cam_key = f"camera_{cam.id}"
+        cam_instance = active_cameras.get(cam_key)
+        camera_states[cam_key] = {
+            "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
+            "name": cam.name
+        }
+
+    now = datetime.now().strftime("%I:%M %p")
+    response_data = {}
+
+    for camera_key, data in camera_states.items():
+        is_open = data["is_open"]
+        readable_name = data["name"]
+        
+        previous_status = last_camera_status.get(camera_key, False)
+        print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
+
+        # OFFLINE ALERT
+        if previous_status and not is_open:
+            print(f"{readable_name} OFFLINE")
+            Alert.objects.create(
+                alert_type="Camera Offline",
+                message=f"{readable_name} Offline"
+            )
+            if can_send_email(f"{camera_key}_offline"):
+                send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
+            last_camera_status_time[camera_key] = now
+
+        # ONLINE ALERT
+        elif not previous_status and is_open:
+            print(f"{readable_name} ONLINE")
+            Alert.objects.create(
+                alert_type="Camera Online",
+                message=f"{readable_name} Online"
+            )
+            if can_send_email(f"{camera_key}_online"):
+                send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
+            last_camera_status_time[camera_key] = None
+
+        last_camera_status[camera_key] = is_open
+        
+        # Populate response data
+        response_data[camera_key] = "Online" if is_open else "Offline"
+        response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
+
+    return JsonResponse(response_data)
+    
+    
+def control_camera(request):
+    camera_id = request.GET.get("camera")
+    action = request.GET.get("action", "").lower()
+    
+    if not camera_id:
+        return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
+        
+    try:
+        cam_obj = Camera.objects.get(id=camera_id)
+    except Camera.DoesNotExist:
+        return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
+
+    cam_key = f"camera_{camera_id}"
+    
+    if action == "start":
+        camera_enabled[cam_key] = True
+        cam_instance = get_camera_instance(cam_obj.id)
+        if cam_instance:
+            CameraLog.objects.create(event=f"{cam_obj.name} Started")
+            return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
+        return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
+
+    if action == "stop":
+        camera_enabled[cam_key] = False
+        if cam_key in active_cameras and active_cameras[cam_key].isOpened():
+            active_cameras[cam_key].release()
+        CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
+        return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
+
+    return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
+
+
+def capture_snapshot(request):
+    camera_id = request.GET.get("camera")
+    if not camera_id:
+        return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
+        
+    try:
+        cam_obj = Camera.objects.get(id=camera_id)
+    except Camera.DoesNotExist:
+        return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
+        
+    cam_key = f"camera_{camera_id}"
+    camera = active_cameras.get(cam_key)
+    
+    if camera and camera.isOpened():
+        success, frame = camera.read()
+        if success:
+            filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
+            path = os.path.join("media", "snapshots", filename)
+            
+            # Ensure directory exists
+            os.makedirs(os.path.dirname(path), exist_ok=True)
+            
+            cv2.imwrite(path, frame)
+            
+            Snapshot.objects.create(
+                image=f"snapshots/{filename}",
+                camera_name=cam_obj.name
+            )
+            CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
+            return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
+            
+    return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
+
+def test_log(request):
+
+    camera_name = (
+        request.POST.get("camera_name") or
+        request.GET.get("camera_name") or
+        "Unknown Camera"
+    )
+
+    RecognitionLog.objects.create(
+        person_name=recognized_name,
+        status="Known",
+        camera_name=camera_name
+    )
+
+    return JsonResponse({
+        "message": "saved"
+    })
+
+def face_count_api(request):
+    return JsonResponse({
+        "count": face_count
+    })
+
+def alerts_api(request):
+    alerts = Alert.objects.order_by('-created_at')[:20]
+    total_count = Alert.objects.count()
+    last_seen = request.session.get('last_alert_count', 0)
+    unread_count = max(0, total_count - last_seen)
+    
+    alert_list = [
+        {
+            "alert_type": alert.alert_type,
+            "message": alert.message,
+            "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
+        }
+        for alert in alerts
+    ]
+    return JsonResponse({
+        "alerts": alert_list,
+        "total_count": total_count,
+        "unread_count": unread_count
+    })
 
 def mark_alerts_read(request):
     if request.method == "POST":
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

You had inaccuracies in your replacement chunks, so you should review the file contents before making further edits. It is important that you get the target content EXACTLY right, otherwise you will make more mistakes.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.
--- MATCH 6 ---
Created At: 2026-06-29T10:36:28Z
Completed At: 2026-06-29T10:36:29Z
File Path: `file:///c:/private/Project/SmartCCTV/dashboard/views.py`
Total Lines: 1804
Total Bytes: 61399
Showing lines 1329 to 1584
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
1329: def test_log(request):
1330: 
1331:     camera_name = (
1332:         request.POST.get("camera_name") or
1333:         request.GET.get("camera_name") or
1334:         "Unknown Camera"
1335:     )
1336: 
1337:     RecognitionLog.objects.create(
1338:         ret, buffer = cv2.imencode(
1339:             ".jpg",
1340:             frame,
1341:             [cv2.IMWRITE_JPEG_QUALITY, 40]
1342:         )
1343: 
1344:         frame_bytes = buffer.tobytes()
1345: 
1346:         yield (
1347:             b'--frame\r\n'
1348:             b'Content-Type: image/jpeg\r\n\r\n'
1349:             + frame_bytes +
1350:             b'\r\n'
1351:         )
1352:       
1353: 
1354: def video_feed(request, camera_id):
1355:     try:
1356:         camera = Camera.objects.get(id=camera_id)
1357:     except Camera.DoesNotExist:
1358:         return HttpResponse("Camera not found", status=404)
1359:         
1360:     cam_key = f"camera_{camera.id}"
1361:     
1362:     return StreamingHttpResponse(
1363:         generate_frames(camera.id, camera.name, cam_key),
1364:         content_type='multipart/x-mixed-replace; boundary=frame'
1365:     )
1366: 
1367: def occupancy_api(request):
1368:     visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
1369:     return JsonResponse({
1370:         "people": people_count,
1371:         "entry": entry_count,
1372:         "exit": exit_count,
1373:         "occupancy": occupancy,
1374:         "visitors_today": visitors_today
1375:     })
1376: 
1377: 
1378: 
1379: def people_count_api(request):
1380: 
1381:     return JsonResponse({
1382:         "count": people_count
1383:     })
1384: 
1385: def camera_is_active(camera, camera_key):
1386:     try:
1387:         if not camera_enabled.get(camera_key, True):
1388:             return False
1389:         if not camera.isOpened():
1390:             return False
1391: 
1392:         last_seen = camera_last_seen.get(camera_key)
1393:         if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
1394:             return True
1395: 
1396:         # Avoid additional reads from an already-streaming capture device.
1397:         # Use the most recent frame timestamp from the generator to determine activity.
1398:         return False
1399:     except Exception:
1400:         return False
1401: 
1402: 
1403: def camera_status(request):
1404:     global last_camera_status
1405:     global last_camera_status_time
1406:     
1407:     cameras = Camera.objects.filter(is_enabled=True)
1408:     camera_states = {}
1409:     
1410:     for cam in cameras:
1411:         cam_key = f"camera_{cam.id}"
1412:         cam_instance = active_cameras.get(cam_key)
1413:         camera_states[cam_key] = {
1414:             "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
1415:             "name": cam.name
1416:         }
1417: 
1418:     now = datetime.now().strftime("%I:%M %p")
1419:     response_data = {}
1420: 
1421:     for camera_key, data in camera_states.items():
1422:         is_open = data["is_open"]
1423:         readable_name = data["name"]
1424:         
1425:         previous_status = last_camera_status.get(camera_key, False)
1426:         print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
1427: 
1428:         # OFFLINE ALERT
1429:         if previous_status and not is_open:
1430:             print(f"{readable_name} OFFLINE")
1431:             Alert.objects.create(
1432:                 alert_type="Camera Offline",
1433:                 message=f"{readable_name} Offline"
1434:             )
1435:             if can_send_email(f"{camera_key}_offline"):
1436:                 send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
1437:             last_camera_status_time[camera_key] = now
1438: 
1439:         # ONLINE ALERT
1440:         elif not previous_status and is_open:
1441:             print(f"{readable_name} ONLINE")
1442:             Alert.objects.create(
1443:                 alert_type="Camera Online",
1444:                 message=f"{readable_name} Online"
1445:             )
1446:             if can_send_email(f"{camera_key}_online"):
1447:                 send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
1448:             last_camera_status_time[camera_key] = None
1449: 
1450:         last_camera_status[camera_key] = is_open
1451:         
1452:         # Populate response data
1453:         response_data[camera_key] = "Online" if is_open else "Offline"
1454:         response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
1455: 
1456:     return JsonResponse(response_data)
1457:     
1458:     
1459: def control_camera(request):
1460:     camera_id = request.GET.get("camera")
1461:     action = request.GET.get("action", "").lower()
1462:     
1463:     if not camera_id:
1464:         return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
1465:         
1466:     try:
1467:         cam_obj = Camera.objects.get(id=camera_id)
1468:     except Camera.DoesNotExist:
1469:         return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
1470: 
1471:     cam_key = f"camera_{camera_id}"
1472:     
1473:     if action == "start":
1474:         camera_enabled[cam_key] = True
1475:         cam_instance = get_camera_instance(cam_obj.id)
1476:         if cam_instance:
1477:             CameraLog.objects.create(event=f"{cam_obj.name} Started")
1478:             return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
1479:         return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
1480: 
1481:     if action == "stop":
1482:         camera_enabled[cam_key] = False
1483:         if cam_key in active_cameras and active_cameras[cam_key].isOpened():
1484:             active_cameras[cam_key].release()
1485:         CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
1486:         return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
1487: 
1488:     return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
1489: 
1490: 
1491: def capture_snapshot(request):
1492:     camera_id = request.GET.get("camera")
1493:     if not camera_id:
1494:         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
1495:         
1496:     try:
1497:         cam_obj = Camera.objects.get(id=camera_id)
1498:     except Camera.DoesNotExist:
1499:         return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
1500:         
1501:     cam_key = f"camera_{camera_id}"
1502:     camera = active_cameras.get(cam_key)
1503:     
1504:     if camera and camera.isOpened():
1505:         success, frame = camera.read()
1506:         if success:
1507:             filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
1508:             path = os.path.join("media", "snapshots", filename)
1509:             
1510:             # Ensure directory exists
1511:             os.makedirs(os.path.dirname(path), exist_ok=True)
1512:             
1513:             cv2.imwrite(path, frame)
1514:             
1515:             Snapshot.objects.create(
1516:                 image=f"snapshots/{filename}",
1517:                 camera_name=cam_obj.name
1518:             )
1519:             CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
1520:             return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
1521:             
1522:     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
1523: 
1524: def test_log(request):
1525: 
1526:     camera_name = (
1527:         request.POST.get("camera_name") or
1528:         request.GET.get("camera_name") or
1529:         "Unknown Camera"
1530:     )
1531: 
1532:     RecognitionLog.objects.create(
1533:         person_name=recognized_name,
1534:         status="Known",
1535:         camera_name=camera_name
1536:     )
1537: 
1538:     return JsonResponse({
1539:         "message": "saved"
1540:     })
1541: 
1542: def face_count_api(request):
1543:     return JsonResponse({
1544:         "count": face_count
1545:     })
1546: 
1547: def alerts_api(request):
1548:     alerts = Alert.objects.order_by('-created_at')[:20]
1549:     total_count = Alert.objects.count()
1550:     last_seen = request.session.get('last_alert_count', 0)
1551:     unread_count = max(0, total_count - last_seen)
1552:     
1553:     alert_list = [
1554:         {
1555:             "alert_type": alert.alert_type,
1556:             "message": alert.message,
1557:             "created_at": alert.created_at.strftime("%B %d, %Y, %I:%M %p")
1558:         }
1559:         for alert in alerts
1560:     ]
1561:     return JsonResponse({
1562:         "alerts": alert_list,
1563:         "total_count": total_count,
1564:         "unread_count": unread_count
1565:     })
1566: 
1567: def mark_alerts_read(request):
1568:     if request.method == "POST":
1569:         request.session['last_alert_count'] = Alert.objects.count()
1570:         return JsonResponse({"status": "success"})
1571:     return JsonResponse({"status": "error"}, status=400)
1572: 
1573: @login_required
1574: @require_http_methods(["POST"])
1575: @csrf_exempt
1576: def delete_alerts(request):
1577:     try:
1578:         import json
1579:         data = json.loads(request.body)
1580:         alert_ids = [int(i) for i in data.get('alert_ids', []) if str(i).isdigit()]
1581:         if not alert_ids:
1582:             return JsonResponse({"status": "error", "message": "No alert IDs provided"}, status=400)
1583:         
1584:         # Delete selected alerts
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.

--- MATCH 7 ---
Created At: 2026-06-29T10:38:46Z
Completed At: 2026-06-29T10:38:48Z
The following changes were made by the replace_file_content tool to: c:\private\Project\SmartCCTV\dashboard\views.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -1326,200 +1326,21 @@
             
     return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
 
-def test_log(request):
-
-    camera_name = (
-        )
-
-        # ==========================
-        # JPEG ENCODE
-        # ==========================
-
-        ret, buffer = cv2.imencode(
-            ".jpg",
-            frame,
-            [cv2.IMWRITE_JPEG_QUALITY, 40]
-        )
-
-        frame_bytes = buffer.tobytes()
-
-        yield (
-            b'--frame\r\n'
-            b'Content-Type: image/jpeg\r\n\r\n'
-            + frame_bytes +
-            b'\r\n'
-        )
-      
-
-def video_feed(request, camera_id):
-    try:
-        camera = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return HttpResponse("Camera not found", status=404)
-        
-    cam_key = f"camera_{camera.id}"
-    
-    return StreamingHttpResponse(
-        generate_frames(camera.id, camera.name, cam_key),
-        content_type='multipart/x-mixed-replace; boundary=frame'
-    )
-
-def occupancy_api(request):
-    visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
-    return JsonResponse({
-        "people": people_count,
-        "entry": entry_count,
-        "exit": exit_count,
-        "occupancy": occupancy,
-        "visitors_today": visitors_today
-    })
-
-
-
-def people_count_api(request):
-
-    return JsonResponse({
-        "count": people_count
-    })
-
-def camera_is_active(camera, camera_key):
-    try:
-        if not camera_enabled.get(camera_key, True):
-            return False
-        if not camera.isOpened():
-            return False
-
-        last_seen = camera_last_seen.get(camera_key)
-        if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
-            return True
-
-        # Avoid additional reads from an already-streaming capture device.
-        # Use the most recent frame timestamp from the generator to determine activity.
-        return False
-    except Exception:
-        return False
-
-
-def camera_status(request):
-    global last_camera_status
-    global last_camera_status_time
-    
-    cameras = Camera.objects.filter(is_enabled=True)
-    camera_states = {}
-    
-    for cam in cameras:
-        cam_key = f"camera_{cam.id}"
-        cam_instance = active_cameras.get(cam_key)
-        camera_states[cam_key] = {
-            "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
-            "name": cam.name
-        }
-
-    now = datetime.now().strftime("%I:%M %p")
-    response_data = {}
-
-    for camera_key, data in camera_states.items():
-        is_open = data["is_open"]
-        readable_name = data["name"]
-        
-        previous_status = last_camera_status.get(camera_key, False)
-        print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
-
-        # OFFLINE ALERT
-        if previous_status and not is_open:
-            print(f"{readable_name} OFFLINE")
-            Alert.objects.create(
-                alert_type="Camera Offline",
-                message=f"{readable_name} Offline"
-            )
-            if can_send_email(f"{camera_key}_offline"):
-                send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
-            last_camera_status_time[camera_key] = now
-
-        # ONLINE ALERT
-        elif not previous_status and is_open:
-            print(f"{readable_name} ONLINE")
-            Alert.objects.create(
-                alert_type="Camera Online",
-                message=f"{readable_name} Online"
-            )
-            if can_send_email(f"{camera_key}_online"):
-                send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
-            last_camera_status_time[camera_key] = None
-
-        last_camera_status[camera_key] = is_open
-        
-        # Populate response data
-        response_data[camera_key] = "Online" if is_open else "Offline"
-        response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
-
-    return JsonResponse(response_data)
-    
-    
-def control_camera(request):
-    camera_id = request.GET.get("camera")
-    action = request.GET.get("action", "").lower()
-    
-    if not camera_id:
-        return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
-        
-    try:
-        cam_obj = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
-
-    cam_key = f"camera_{camera_id}"
-    
-    if action == "start":
-        camera_enabled[cam_key] = True
-        cam_instance = get_camera_instance(cam_obj.id)
-        if cam_instance:
-            CameraLog.objects.create(event=f"{cam_obj.name} Started")
-            return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
-        return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
-
-    if action == "stop":
-        camera_enabled[cam_key] = False
-        if cam_key in active_cameras and active_cameras[cam_key].isOpened():
-            active_cameras[cam_key].release()
-        CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
-        return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
-
-    return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
-
-
-def capture_snapshot(request):
-    camera_id = request.GET.get("camera")
-    if not camera_id:
-        return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
-        
-    try:
-        cam_obj = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
-        
-    cam_key = f"camera_{camera_id}"
-    camera = active_cameras.get(cam_key)
-    
-    if camera and camera.isOpened():
-        success, frame = camera.read()
-        if success:
-            filename = datetime.now().strftime("%Y%m%d_%H%M%S.jpg")
-            path = os.path.join("media", "snapshots", filename)
-            
-            # Ensure directory exists
-            os.makedirs(os.path.dirname(path), exist_ok=True)
-            
-            cv2.imwrite(path, frame)
-            
-            Snapshot.objects.create(
-                image=f"snapshots/{filename}",
-                camera_name=cam_obj.name
-            )
-            CameraLog.objects.create(event=f"{cam_obj.name} Snapshot Captured")
-            return JsonResponse({"status": "success", "message": f"{cam_obj.name} Snapshot Saved"})
-            
-    return JsonResponse({"status": "error", "message": "Camera not ready or read failed"}, status=500)
+def test_log(request)    camera_name = (
+        request.POST.get("camera_name") or
+        request.GET.get("camera_name") or
+        "Unknown Camera"
+    )
+
+    RecognitionLog.objects.create(
+        person_name=recognized_name,
+        status="Known",
+        camera_name=camera_name
+    )
+
+    return JsonResponse({
+        "message": "saved"
+    })=500)
 
 def test_log(request):
 
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
--- MATCH 8 ---
Created At: 2026-06-29T11:07:06Z
Completed At: 2026-06-29T11:07:40Z
The following changes were made by the multi_replace_file_content tool to: c:\private\Project\SmartCCTV\dashboard\views.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -163,1137 +163,6 @@
         "cameras": cameras,
         "online": online_count,
         "offline": offline_count,
-        "known_count": known_count,
-        "unknown_count": unknown_count,
-    }
-
-    return render(request, "dashboard/index.html", context)
-
-@login_required
-def live_monitoring(request):
-    cameras = Camera.objects.filter(is_enabled=True).order_by("camera_no")
-    width, height, fps = 640, 480, 30
-    context = {
-        "cameras": cameras,
-        "width": width,
-        "height": height,
-        "fps": fps,
-    }
-    return render(request, "dashboard/live_monitoring.html", context)
-
-@login_required
-def cameras(request):
-    cameras = Camera.objects.all().order_by("camera_no")
-    for cam in cameras:
-        cam_key = f"camera_{cam.id}"
-        cam_instance = active_cameras.get(cam_key)
-        cam.is_online = camera_is_active(cam_instance, cam_key) if cam_instance else False
-        
-    context = {"cameras": cameras}
-    return render(request, "dashboard/cameras.html", context)
-
-@login_required
-def delete_camera(request, camera_id):
-    if request.method == "POST":
-        try:
-            cam = Camera.objects.get(id=camera_id)
-            cam_key = f"camera_{camera_id}"
-            
-            # release camera if open
-            if cam_key in active_cameras:
-                if active_cameras[cam_key].isOpened():
-                    active_cameras[cam_key].release()
-                del active_cameras[cam_key]
-                
-            cam.delete()
-            return JsonResponse({"status": "success", "message": "Camera deleted successfully!"})
-        except Camera.DoesNotExist:
-            return JsonResponse({"status": "error", "message": "Camera not found"}, status=404)
-        except Exception as e:
-            return JsonResponse({"status": "error", "message": str(e)}, status=500)
-    return JsonResponse({"status": "error", "message": "Invalid request method."}, status=400)
-
-@login_required
-def add_camera(request):
-    if request.method == "POST":
-        name = request.POST.get("name")
-        url = request.POST.get("url", "")
-        cam_type = request.POST.get("type", "Webcam")
-        
-        if not name:
-            return JsonResponse({"status": "error", "message": "Camera name is required."}, status=400)
-            
-        try:
-            # Determine next camera number
-            last_cam = Camera.objects.order_by('-camera_no').first()
-            next_no = (last_cam.camera_no + 1) if last_cam else 1
-            
-            Camera.objects.create(
-                name=name,
-                source_url=url,
-                camera_type=cam_type,
-                camera_no=next_no,
-                is_enabled=True
-            )
-            return JsonResponse({"status": "success", "message": "Camera added successfully!"})
-        except Exception as e:
-            return JsonResponse({"status": "error", "message": str(e)}, status=500)
-            
-    return JsonResponse({"status": "error", "message": "Invalid request method."}, status=400)
-
-@login_required
-def ai_events(request):
-    alerts = Alert.objects.order_by('-created_at')[:50]
-    logs = CameraLog.objects.order_by('-created_at')[:50]
-    context = {"alerts": alerts, "logs": logs}
-    return render(request, "dashboard/ai_events.html", context)
-
-@login_required
-def attendance_view(request):
-    attendance = Attendance.objects.all()
-    
-    status = request.GET.get('status')
-    if status:
-        attendance = attendance.filter(status__iexact=status)
-        
-    date_filter = request.GET.get('date')
-    if date_filter:
-        attendance = attendance.filter(date=date_filter)
-        
-    attendance = attendance.order_by('-date', '-entry_time')[:50]
-    context = {
-        "attendance": attendance,
-        "current_status": status,
-        "current_date": date_filter
-    }
-    return render(request, "dashboard/attendance.html", context)
-
-@login_required
-def export_attendance_csv(request):
-    import csv
-    from django.http import HttpResponse
-
-    response = HttpResponse(content_type='text/csv')
-    response['Content-Disposition'] = 'attachment; filename="attendance_records.csv"'
-    
-    writer = csv.writer(response)
-    writer.writerow(['Employee / Person', 'Date', 'Entry Time', 'Exit Time', 'Status'])
-    
-    attendance = Attendance.objects.all()
-    
-    status = request.GET.get('status')
-    if status:
-        attendance = attendance.filter(status__iexact=status)
-        
-    date_filter = request.GET.get('date')
-    if date_filter:
-        attendance = attendance.filter(date=date_filter)
-        
-    attendance = attendance.order_by('-date', '-entry_time')
-    
-    for row in attendance:
-        entry = row.entry_time.strftime("%H:%M:%S") if row.entry_time else "--"
-        exit = row.exit_time.strftime("%H:%M:%S") if row.exit_time else "--"
-        writer.writerow([row.employee_name, row.date, entry, exit, row.status])
-        
-    return response
-
-@login_required
-def visitors_view(request):
-    q = request.GET.get('q', '')
-    if q:
-        visitors = Visitorlogo.objects.filter(visitor_name__icontains=q).order_by('-detection_time')[:50]
-    else:
-        visitors = Visitorlogo.objects.order_by('-detection_time')[:50]
-    context = {"visitors": visitors}
-    return render(request, "dashboard/visitors.html", context)
-
-@login_required
-def snapshots_view(request):
-    snapshots = Snapshot.objects.order_by('-created_at')[:50]
-    context = {"snapshots": snapshots}
-    return render(request, "dashboard/snapshots.html", context)
-
-@login_required
-def logs_view(request):
-    logs = CameraLog.objects.order_by('-created_at')[:50]
-    context = {"logs": logs}
-    return render(request, "dashboard/logs.html", context)
-
-@login_required
-def settings_view(request):
-    return render(request, "dashboard/settings.html", {})
-
-@login_required
-def profile_view(request):
-    if request.method == 'POST':
-        user = request.user
-        user.first_name = request.POST.get('first_name', user.first_name)
-        user.last_name = request.POST.get('last_name', user.last_name)
-        user.email = request.POST.get('email', user.email)
-        user.save()
-        from django.contrib import messages
-        messages.success(request, 'Profile updated successfully!')
-        from django.shortcuts import redirect
-        return redirect('profile')
-    return render(request, "dashboard/profile.html")
-
-@login_required
-def alerts_page(request):
-    alerts = Alert.objects.order_by('-created_at')[:100]
-    context = {"alerts": alerts}
-    return render(request, "dashboard/alerts_page.html", context)
-
-@login_required
-def export_alerts_csv(request):
-    import csv
-    from django.http import HttpResponse
-
-    response = HttpResponse(content_type='text/csv')
-    response['Content-Disposition'] = 'attachment; filename="system_alerts.csv"'
-    
-    writer = csv.writer(response)
-    writer.writerow(['Severity', 'Alert Type', 'Message', 'Timestamp'])
-    
-    alerts = Alert.objects.all().order_by('-created_at')
-    
-    for alert in alerts:
-        # Determine severity based on logic in template
-        severity = "Info"
-        if alert.alert_type in ["Intrusion", "Blacklisted Person"]:
-            severity = "Critical"
-        elif alert.alert_type == "Unknown Face":
-            severity = "Warning"
-            
-        timestamp = f" {alert.created_at.strftime('%Y-%m-%d %H:%M:%S')}" if alert.created_at else "--"
-        writer.writerow([severity, alert.alert_type, alert.message, timestamp])
-        
-    return response
-
-@login_required
-def export_visitors_csv(request):
-    import csv
-    from django.http import HttpResponse
-
-    response = HttpResponse(content_type='text/csv')
-    response['Content-Disposition'] = 'attachment; filename="visitor_logs.csv"'
-    
-    writer = csv.writer(response)
-    writer.writerow(['Visitor Name', 'Camera', 'Detection Time'])
-    
-    visitors = Visitorlogo.objects.all().order_by('-detection_time')
-    
-    for visitor in visitors:
-        timestamp = f" {visitor.detection_time.strftime('%Y-%m-%d %H:%M:%S')}" if visitor.detection_time else "--"
-        writer.writerow([visitor.visitor_name, visitor.camera_name, timestamp])
-        
-    return response
-
-@login_required
-def reports_view(request):
-    return render(request, "dashboard/reports.html")
-
-import base64
-from django.core.files.base import ContentFile
-from django.contrib import messages
-from django.shortcuts import render, redirect
-
-@login_required
-def face_register(request):
-    if request.method == "POST":
-        name = request.POST.get("person_name")
-        role = request.POST.get("person_role")
-        gender = request.POST.get("person_gender")
-        
-        employee = Employee(name=name, role=role, gender=gender)
-        
-        # Helper function to process each angle
-        def process_image(angle):
-            base64_data = request.POST.get(f"base64_{angle}")
-            file_data = request.FILES.get(f"image_{angle}")
-            
-            if base64_data:
-                # format: "data:image/jpeg;base64,/9j/4AAQ..."
-                fmt, imgstr = base64_data.split(';base64,') 
-                ext = fmt.split('/')[-1]
-                data = ContentFile(base64.b64decode(imgstr), name=f"{name}_{angle}.{ext}")
-                setattr(employee, f"image_{angle}", data)
-            elif file_data:
-                setattr(employee, f"image_{angle}", file_data)
-
-        process_image("front")
-        process_image("right")
-        process_image("left")
-        
-        employee.save()
-        messages.success(request, f"Successfully registered {name}!")
-        return redirect("face_register")
-
-    registered_faces = Employee.objects.all().order_by('-id')
-    return render(request, "dashboard/face_register.html", {"registered_faces": registered_faces})
-
-@login_required
-@require_http_methods(["POST"])
-def delete_face(request, pk):
-    try:
-        employee = Employee.objects.get(id=pk)
-        name = employee.name
-        employee.delete()
-        messages.success(request, f"Successfully deleted {name}.")
-    except Employee.DoesNotExist:
-        messages.error(request, "Employee not found.")
-    return redirect("face_register")
-
-@login_required
-def wanted_persons(request):
-    if request.method == "POST":
-        name = request.POST.get("wanted_name")
-        image = request.FILES.get("wanted_image")
-        reason = request.POST.get("wanted_reason", "")
-
-        if name and image:
-            BlacklistPerson.objects.create(name=name, image=image)
-            messages.success(request, f"'{name}' has been added to the watchlist. You will be alerted when detected on any CCTV.")
-        else:
-            messages.error(request, "Please provide both a name and an image.")
-
-        return redirect("wanted_persons")
-
-    persons = BlacklistPerson.objects.all().order_by('-added_at')
-    return render(request, "dashboard/wanted_persons.html", {"wanted_persons": persons})
-
-@login_required
-@require_http_methods(["POST"])
-def delete_wanted(request, pk):
-    try:
-        person = BlacklistPerson.objects.get(id=pk)
-        name = person.name
-        if person.image:
-            person.image.delete(save=False)
-        person.delete()
-        messages.success(request, f"'{name}' has been removed from the watchlist.")
-    except BlacklistPerson.DoesNotExist:
-        messages.error(request, "Person not found.")
-    return redirect("wanted_persons")
-
-snapshot_requests = {
-    "camera1": False,
-    "camera2": False,
-}
-
-@login_required
-def capture_snapshot(request):
-    camera_id = request.GET.get('camera', '1')
-    cam_key = f"camera_{camera_id}"
-    global snapshot_requests
-    snapshot_requests[cam_key] = True
-    return JsonResponse({"status": "success", "message": f"Snapshot requested for camera {camera_id}."})
-
-@login_required
-def download_snapshot(request, pk):
-    try:
-        from django.shortcuts import get_object_or_404
-        from django.http import HttpResponse, JsonResponse
-        import io
-        from PIL import Image
-
-        snapshot = get_object_or_404(Snapshot, pk=pk)
-        fmt = request.GET.get('format', 'jpg').lower()
-        
-        # Determine content type and Pillow format
-        if fmt == 'png':
-            content_type = 'image/png'
-            pil_format = 'PNG'
-        elif fmt == 'pdf':
-            content_type = 'application/pdf'
-            pil_format = 'PDF'
-        elif fmt == 'bmp':
-            content_type = 'image/bmp'
-            pil_format = 'BMP'
-        else:
-            fmt = 'jpg'
-            content_type = 'image/jpeg'
-            pil_format = 'JPEG'
-            
-        with Image.open(snapshot.image.path) as img:
-            # Ensure RGB if needed
-            if img.mode in ("RGBA", "P"):
-                img = img.convert("RGB")
-            
-            buffer = io.BytesIO()
-            img.save(buffer, format=pil_format)
-            
-            response = HttpResponse(buffer.getvalue(), content_type=content_type)
-            filename = f"snapshot_{snapshot.id}.{fmt}"
-            response['Content-Disposition'] = f'attachment; filename="{filename}"'
-            return response
-    except Exception as e:
-        return JsonResponse({"status": "error", "message": str(e)}, status=400)
-
-
-@login_required
-def delete_snapshot(request, pk):
-    from django.shortcuts import get_object_or_404, redirect
-    from django.contrib import messages
-    if request.method == 'POST':
-        snapshot = get_object_or_404(Snapshot, pk=pk)
-        if snapshot.image:
-            snapshot.image.delete(save=False)
-        snapshot.delete()
-    return redirect('snapshots')
-
-def generate_frames(camera_id, camera_name, camera_key):
-
-    global people_count
-    global face_count
-    # global recognized_name
-    global last_unknown_time
-    global last_recognized_time
-    global last_recognized_name
-    global last_occupancy_time
-    global camera_last_seen
-    # global emotion
-    global name_id_map, next_person_id, unknown_counter
-    
-    frame_count = 0
-
-    recognized_name = "Unknown"
-    emotion = "Unknown"
-    current_person_id = None
-    
-    while True:
-        is_blacklisted = False
-
-        if not camera_enabled.get(camera_key, True):
-            camera = active_cameras.get(camera_key)
-            if camera is not None and camera.isOpened():
-                camera.release()
-            blank = np.zeros((480, 640, 3), dtype=np.uint8)
-            cv2.putText(
-                blank,
-                "Camera Stopped",
-                (20, 260),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                1,
-                (0, 0, 255),
-                2
-            )
-            ret, buffer = cv2.imencode(
-                ".jpg",
-                blank,
-                [cv2.IMWRITE_JPEG_QUALITY, 40]
-            )
-            frame_bytes = buffer.tobytes()
-            yield (
-                b'--frame\r\n'
-                b'Content-Type: image/jpeg\r\n\r\n'
-                + frame_bytes +
-                b'\r\n'
-            )
-            time.sleep(0.2)
-            continue
-
-        camera = active_cameras.get(camera_key)
-        if camera is None or not camera.isOpened():
-            camera = get_camera_instance(camera_id)
-            
-        success, frame = False, None
-        if camera is not None and camera.isOpened():
-            success, frame = camera.read()
-
-        if camera_key == "camera2":
-            print(
-                "CAM2:",
-                success,
-                frame.shape if success else None
-            )
-
-        if not success or frame is None or (hasattr(frame, 'size') and frame.size == 0):
-            print(f"{camera_name} read failure: success={success}, frame={None if frame is None else getattr(frame, 'shape', 'unknown')}")
-            
-            if camera is not None:
-                camera.release()
-            if camera_key in active_cameras:
-                del active_cameras[camera_key]
-                
-            # Camera not available – yield a placeholder and keep retrying
-            placeholder = np.zeros((480, 640, 3), dtype=np.uint8)
-            placeholder[:] = (30, 30, 30)  # dark background
-            cv2.putText(
-                placeholder,
-                "Camera Not Available",
-                (100, 220),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                1.0,
-                (0, 100, 255),
-                2,
-            )
-            cv2.putText(
-                placeholder,
-                "USB Camera Not Connected",
-                (120, 260),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                0.7,
-                (180, 180, 180),
-                1,
-            )
-            cv2.putText(
-                placeholder,
-                "Retrying...",
-                (240, 300),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                0.6,
-                (0, 200, 100),
-                1,
-            )
-            _ret, _buf = cv2.imencode(
-                ".jpg",
-                placeholder,
-                [cv2.IMWRITE_JPEG_QUALITY, 50]
-            )
-            yield (
-                b'--frame\r\n'
-                b'Content-Type: image/jpeg\r\n\r\n'
-                + _buf.tobytes() +
-                b'\r\n'
-            )
-            time.sleep(2)  # wait before retrying
-            continue
-
-        camera_last_seen[camera_key] = datetime.now()
-        frame_count += 1
-
-        # ==========================
-        # SNAPSHOT HANDLING
-        # ==========================
-        if snapshot_requests.get(camera_key):
-            try:
-                now = datetime.now()
-                filename = now.strftime("%Y%m%d_%H%M%S") + ".jpg"
-                snap_folder = os.path.join("media", "snapshots")
-                os.makedirs(snap_folder, exist_ok=True)
-                snap_path = os.path.join(snap_folder, filename)
-                
-                cv2.imwrite(snap_path, frame)
-                
-                Snapshot.objects.create(
-                    image=f"snapshots/{filename}",
-                    camera_name=camera_name
-                )
-                print(f"Snapshot saved for {camera_name}")
-            except Exception as e:
-                print(f"Error saving snapshot: {e}")
-            
-            snapshot_requests[camera_key] = False
-
-        # ==========================
-        # YOLO OBJECT DETECTION
-        # ==========================
-
-        yolo_results = model(frame, 
-                             imgsz=320, 
-                             conf=0.25,
-                             verbose=False)
-        phone_count = 0
-        object_labels = []
-        people_count = 0
-
-        if len(yolo_results) > 0 and len(yolo_results[0].boxes) > 0:
-            result = yolo_results[0]
-
-            for xyxy, cls_id, conf in zip(
-                result.boxes.xyxy,
-                result.boxes.cls,
-                result.boxes.conf
-            ):
-                x1, y1, x2, y2 = map(int, xyxy.tolist())
-                class_id = int(cls_id.item())
-                label = result.names[class_id]
-                if label.lower() == "person":
-                    people_count += 1
-
-                object_labels.append(label)
-
-                cv2.rectangle(
-                    frame,
-                    (x1, y1),
-                    (x2, y2),
-                    (0, 255, 0),
-                    2
-                )
-
-                cv2.putText(
-                    frame,
-                    f"{label} {conf:.2f}",
-                    (x1, y1 - 10),
-                    cv2.FONT_HERSHEY_SIMPLEX,
-                    0.6,
-                    (0, 255, 0),
-                    2
-                )
-
-                if label.lower() in ["cell phone", "phone", "mobile phone"]:
-                    phone_count += 1
-
-        phone_text = f"Phones: {phone_count}"
-
-
-
-       
-
-        # ==========================
-        # FACE DETECTION
-        # ==========================
-
-        gray = cv2.cvtColor(
-            frame,
-            cv2.COLOR_BGR2GRAY
-        )
-
-        faces = face_cascade.detectMultiScale(
-            gray,
-            scaleFactor=1.1,
-            minNeighbors=4,
-            minSize=(50, 50)
-        )
-
-        face_count = len(faces)
-        # people_count = face_count
-
-        # ==========================
-        # OCCUPANCY ALERT
-        # ==========================
-
-        MAX_PEOPLE = 2
-
-        if people_count > MAX_PEOPLE:
-
-            now = datetime.now()
-
-            if (
-                last_occupancy_time is None
-                or
-                now - last_occupancy_time > timedelta(seconds=60)
-            ):
-
-                Alert.objects.create(
-
-                    alert_type="Occupancy Alert",
-
-                    message=f"{people_count} people detected in {camera_name}"
-
-                )
-
-                print(
-                    f"Occupancy Alert: {people_count} people"
-                )
-
-                last_occupancy_time = now
-
-        # only reset when face disappears
-        if len(faces) == 0:
-            recognized_name = "Unknown"
-
-        # ==========================
-        # FACE RECOGNITION
-        # ==========================
-        if len(faces) > 0 and frame_count % 15 == 0:
-
-            print("Recognition Block Entered")
-
-            try:
-
-                x, y, w, h = faces[0]
-
-                x1 = max(0, x)
-                y1 = max(0, y)
-
-                x2 = x1 + w
-                y2 = y1 + h
-
-                face_img = frame[y1:y2, x1:x2]
-
-                face_img = cv2.resize(
-                    face_img,
-                    (224, 224)
-                )
-
-                if face_img.size > 0:
-
-                    cv2.imwrite(
-                        "media/current_face.jpg",
-                        face_img
-                    )
-
-                    print("Recognition Started")
-
-                    recognized_name = recognize_face(
-                        "media/current_face.jpg"
-                    )
-                    emotion = detect_emotion(
-                        "media/current_face.jpg"
-                    )
-
-                    blacklisted_person = check_blacklist(
-                        "media/current_face.jpg"
-                    )
-                    
-                    print("BLACKLIST RESULT =", blacklisted_person)
-
-                    if blacklisted_person:
-
-                        is_blacklisted = True
-
-                        alert = Alert.objects.create(
-                            alert_type="Blacklisted Person",
-                            message=f"{blacklisted_person} detected in {camera_name}"
-                        )
-
-                        print("ALERT ID =", alert.id)
-
-                        print(f"BLACKLIST DETECTED: {blacklisted_person}")
-
-                        if can_send_email(
-                            f"blacklist_{blacklisted_person}"
-                        ):
-
-                            send_alert_email(
-                                "🚨 BLACKLISTED PERSON DETECTED",
-                                f"{blacklisted_person} detected in {camera_name}"
-                            )
-                        filename = datetime.now().strftime(
-                            "blacklist_%Y%m%d_%H%M%S.jpg"
-                        )
-
-                        path = os.path.join(
-                            "media",
-                            "blacklist_events",
-                            filename
-                        )
-
-                        os.makedirs(
-                            "media/blacklist_events",
-                            exist_ok=True
-                        )
-
-                        cv2.imwrite(
-                            path,
-                            frame
-                        )
-                    # assign or reuse a numeric id for the recognized person
-                    if recognized_name != "Unknown":
-                        if recognized_name not in name_id_map:
-                            name_id_map[recognized_name] = next_person_id
-                            next_person_id += 1
-                        current_person_id = name_id_map[recognized_name]
-                    else:
-                        # unknowns get a running U<number>
-                        unknown_counter += 1
-                        current_person_id = f"U{unknown_counter}"
-
-                    print("Emotion =", emotion)
-
-                    today = date.today()
-
-                    # if recognized_name != "Unknown":
-
-                    #     Alert.objects.create(
-                    #         alert_type="Known Face",
-                    #         message=f"{recognized_name} detected in {camera_name}"
-                    #     )
-
-                    #     if can_send_email(f"known_{recognized_name}"):
-
-                    #         send_alert_email(
-                    #             "Known Person Detected",
-                    #             f"{recognized_name} detected in {camera_name}"
-                    #         )
-
-                    attendance_record = Attendance.objects.filter(
-                        employee_name=recognized_name,
-                        date=today
-                    ).first()
-
-                    if not attendance_record:
-                        Attendance.objects.create(
-                            employee_name=recognized_name,
-                            status="Present"
-                        )
-                    else:
-                        # Update the exit time on subsequent detections
-                        attendance_record.exit_time = datetime.now().time()
-                        attendance_record.save()
-
-                    print("Recognized =", recognized_name)
-
-                    now = datetime.now()
-                    should_save_visitor = False
-                    visitor_name = recognized_name
-                    alert_type = None
-                    alert_message = None
-
-                    if recognized_name == "Unknown":
-                        alert_type = "Unknown Face"
-                        alert_message = "Unknown person detected"
-                        should_save_visitor = (
-                            last_unknown_time is None or
-                            now - last_unknown_time > timedelta(seconds=30)
-                        )
-                    else:
-                        if (
-                            last_recognized_name != recognized_name or
-                            last_recognized_time is None or
-                            now - last_recognized_time > timedelta(seconds=30)
-                        ):
-                            should_save_visitor = True
-
-                    if should_save_visitor:
-
-                        if alert_type is not None:
-
-                            Alert.objects.create(
-                                alert_type="Unknown Face",
-                                message=f"Unknown person detected in {camera_name}"
-                            )
-
-                            print("Unknown Visitor Detected")
-
-                            if can_send_email("unknown_face"):
-
-                                send_alert_email(
-                                    "Unknown Face Detected",
-                                    f"Unknown person detected in {camera_name}"
-                                )
-
-                        else:
-
-                            Alert.objects.create(
-                                alert_type="Known Face",
-                                message=f"{recognized_name} detected in {camera_name}"
-                            )
-
-                            print(f"Known Visitor Detected: {recognized_name}")
-
-                            if can_send_email(f"known_{recognized_name}"):
-
-                                send_alert_email(
-                                    "Known Person Detected",
-                                    f"{recognized_name} detected in {camera_name}"
-                                )
-
-                        filename = now.strftime(
-                            "%Y%m%d_%H%M%S"
-                        ) + ".jpg"
-
-                        visitor_folder = os.path.join(
-                            "media",
-                            "visitors"
-                        )
-
-                        os.makedirs(
-                            visitor_folder,
-                            exist_ok=True
-                        )
-
-                        visitor_path = os.path.join(
-                            visitor_folder,
-                            filename
-                        )
-
-                        cv2.imwrite(
-                            visitor_path,
-                            frame
-                        )
-
-                        visitor = Visitorlogo.objects.create(
-                        visitor_name=visitor_name,
-                        Snapshot=f"visitors/{filename}",
-                        camera_name=camera_name
-                    )
-
-                        print(
-                            "Visitor Saved:",
-                            visitor.id
-                        )
-
-                        if recognized_name == "Unknown":
-                            last_unknown_time = now
-                        else:
-                            last_recognized_time = now
-                            last_recognized_name = recognized_name
-
-                        
-
-            except Exception as e:
-
-                print("Recognition Error:", e)
-
-                recognized_name = "Unknown"
-        # ==========================
-        # DRAW FACE BOX
-        # ==========================
-        if is_blacklisted:
-
-            cv2.putText(
-                frame,
-                "BLACKLISTED PERSON",
-                (50, 50),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                1,
-                (0, 0, 255),
-                3
-            )
-        for (x, y, w, h) in faces:
-
-            if is_blacklisted:
-
-                box_color = (0, 0, 255)
-                text_color = (0, 0, 255)
-
-            elif recognized_name != "Unknown":
-
-                box_color = (0, 255, 0)
-                text_color = (0, 255, 0)
-
-            else:
-
-                box_color = (0, 165, 255)
-                text_color = (0, 165, 255)
-
-
-            cv2.rectangle(
-                frame,
-                (x, y),
-                (x + w, y + h),
-                box_color,
-                2
-            )
-
-            # Display name and assigned ID
-            id_text = f"ID: {current_person_id}" if current_person_id is not None else "ID: -"
-            cv2.putText(
-                frame,
-                f"{recognized_name} | {id_text}",
-                (x, y - 20),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                0.7,
-                text_color,
-                2
-            )
-
-            # Emotion
-            cv2.putText(
-                frame,
-                f"Emotion: {emotion}",
-                (x, y - 5),
-                cv2.FONT_HERSHEY_SIMPLEX,
-                0.6,
-                (255, 255, 0),
-                2
-            )
-
-        # ==========================
-        # STATS
-        # ==========================
-
-        cv2.putText(
-            frame,
-            f"Faces: {face_count}",
-            (10, 30),
-            cv2.FONT_HERSHEY_SIMPLEX,
-            0.8,
-            (0, 255, 255),
-            2
-        )
-
-        # cv2.putText(
-        #     frame,
-        #     f"Name: {recognized_name}",
-        #     (10, 70),
-        #     cv2.FONT_HERSHEY_SIMPLEX,
-        #     0.8,
-        #     (0, 255, 0),
-        #     2
-        # )
-
-        # cv2.putText(
-        #     frame,
-        #     f"Emotion: {emotion}",
-        #     (10, 150),
-        #     cv2.FONT_HERSHEY_SIMPLEX,
-        #     0.8,
-        #     (255, 0, 255),
-        #     2
-        # )
-
-        cv2.putText(
-            frame,
-            phone_text,
-            (10, 110),
-            cv2.FONT_HERSHEY_SIMPLEX,
-            0.8,
-            (0, 255, 255),
-            2
-        )
-
-        # ==========================
-        # JPEG ENCODE
-        # ==========================
-
-        ret, buffer = cv2.imencode(
-            ".jpg",
-            frame,
-            [cv2.IMWRITE_JPEG_QUALITY, 40]
-        )
-
-        frame_bytes = buffer.tobytes()
-
-        yield (
-            b'--frame\r\n'
-            b'Content-Type: image/jpeg\r\n\r\n'
-            + frame_bytes +
-            b'\r\n'
-        )
-      
-
-def video_feed(request, camera_id):
-    try:
-        camera = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return HttpResponse("Camera not found", status=404)
-        
-    cam_key = f"camera_{camera.id}"
-    
-    return StreamingHttpResponse(
-        generate_frames(camera.id, camera.name, cam_key),
-        content_type='multipart/x-mixed-replace; boundary=frame'
-    )
-
-def occupancy_api(request):
-    visitors_today = Visitorlogo.objects.filter(detection_time__date=date.today()).count()
-    return JsonResponse({
-        "people": people_count,
-        "entry": entry_count,
-        "exit": exit_count,
-        "occupancy": occupancy,
-        "visitors_today": visitors_today
-    })
-
-
-
-def people_count_api(request):
-
-    return JsonResponse({
-        "count": people_count
-    })
-
-def camera_is_active(camera, camera_key):
-    try:
-        if not camera_enabled.get(camera_key, True):
-            return False
-        if not camera.isOpened():
-            return False
-
-        last_seen = camera_last_seen.get(camera_key)
-        if last_seen and datetime.now() - last_seen <= timedelta(seconds=10):
-            return True
-
-        # Avoid additional reads from an already-streaming capture device.
-        # Use the most recent frame timestamp from the generator to determine activity.
-        return False
-    except Exception:
-        return False
-
-
-def camera_status(request):
-    global last_camera_status
-    global last_camera_status_time
-    
-    cameras = Camera.objects.filter(is_enabled=True)
-    camera_states = {}
-    
-    for cam in cameras:
-        cam_key = f"camera_{cam.id}"
-        cam_instance = active_cameras.get(cam_key)
-        camera_states[cam_key] = {
-            "is_open": camera_is_active(cam_instance, cam_key) if cam_instance else False,
-            "name": cam.name
-        }
-
-    now = datetime.now().strftime("%I:%M %p")
-    response_data = {}
-
-    for camera_key, data in camera_states.items():
-        is_open = data["is_open"]
-        readable_name = data["name"]
-        
-        previous_status = last_camera_status.get(camera_key, False)
-        print(f"{camera_key} | Previous={previous_status} | Current={is_open}")
-
-        # OFFLINE ALERT
-        if previous_status and not is_open:
-            print(f"{readable_name} OFFLINE")
-            Alert.objects.create(
-                alert_type="Camera Offline",
-                message=f"{readable_name} Offline"
-            )
-            if can_send_email(f"{camera_key}_offline"):
-                send_alert_email("Camera Offline Alert", f"{readable_name} is Offline.")
-            last_camera_status_time[camera_key] = now
-
-        # ONLINE ALERT
-        elif not previous_status and is_open:
-            print(f"{readable_name} ONLINE")
-            Alert.objects.create(
-                alert_type="Camera Online",
-                message=f"{readable_name} Online"
-            )
-            if can_send_email(f"{camera_key}_online"):
-                send_alert_email("Camera Online Alert", f"{readable_name} is Online again.")
-            last_camera_status_time[camera_key] = None
-
-        last_camera_status[camera_key] = is_open
-        
-        # Populate response data
-        response_data[camera_key] = "Online" if is_open else "Offline"
-        response_data[f"{camera_key}_time"] = last_camera_status_time.get(camera_key)
-
-    return JsonResponse(response_data)
-    
-    
-def control_camera(request):
-    camera_id = request.GET.get("camera")
-    action = request.GET.get("action", "").lower()
-    
-    if not camera_id:
-        return JsonResponse({"message": "Camera ID required", "status": "error"}, status=400)
-        
-    try:
-        cam_obj = Camera.objects.get(id=camera_id)
-    except Camera.DoesNotExist:
-        return JsonResponse({"message": "Camera not found", "status": "error"}, status=404)
-
-    cam_key = f"camera_{camera_id}"
-    
-    if action == "start":
-        camera_enabled[cam_key] = True
-        cam_instance = get_camera_instance(cam_obj.id)
-        if cam_instance:
-            CameraLog.objects.create(event=f"{cam_obj.name} Started")
-            return JsonResponse({"message": f"{cam_obj.name} started", "status": "started"})
-        return JsonResponse({"message": f"Failed to start {cam_obj.name}", "status": "error"}, status=500)
-
-    if action == "stop":
-        camera_enabled[cam_key] = False
-        if cam_key in active_cameras and active_cameras[cam_key].isOpened():
-            active_cameras[cam_key].release()
-        CameraLog.objects.create(event=f"{cam_obj.name} Stopped")
-        return JsonResponse({"message": f"{cam_obj.name} stopped", "status": "stopped"})
-
-    return JsonResponse({"message": "Invalid camera action", "status": "error"}, status=400)
-
-
-def capture_snapshot(request):
     camera_id = request.GET.get("camera")
     if not camera_id:
         return JsonResponse({"status": "error", "message": "Camera ID required"}, status=400)
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.
--- MATCH 9 ---
Created At: 2026-06-29T11:14:34Z
Completed At: 2026-06-29T11:14:36Z
The following changes were made by the replace_file_content tool to: c:\private\Project\SmartCCTV\dashboard\apps.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -5,32 +5,22 @@
     name = "dashboard"
 
     def ready(self):
-        if not os.path.exists("restored8.txt"):
-            import json, re
+        if not os.path.exists("restored9.txt"):
+            import json, os
             transcript_path = r"C:\Users\Admin\.gemini\antigravity-ide\brain\896911f3-b771-4e6a-976d-315f11c23b29\.system_generated\logs\transcript_full.jsonl"
             try:
-                file_lines = {}
+                diff_content = ""
                 with open(transcript_path, 'r', encoding='utf-8') as f:
                     for line in f:
-                        if '"type":"TOOL_RESPONSE"' in line:
+                        if '"type":"TOOL_RESPONSE"' in line and '[diff_block_start]' in line and 'views.py' in line:
                             data = json.loads(line)
                             content = data.get('content', '') or data.get('output', '')
-                            if "File Path: `file:///c:/private/Project/SmartCCTV/dashboard/views.py`" in content:
-                                for cline in content.split('\n'):
-                                    m = re.match(r'^(\d+):\s(.*)', cline)
-                                    if m:
-                                        file_lines[int(m.group(1))] = m.group(2)
-                
-                max_line = max(file_lines.keys()) if file_lines else 0
-                out_lines = []
-                for i in range(1, max_line + 1):
-                    out_lines.append(file_lines.get(i, f"# MISSING LINE {i}"))
-
-                with open("views_full_restored.py", "w", encoding='utf-8') as f:
-                    f.write('\n'.join(out_lines))
-                with open("restored8.txt", "w", encoding='utf-8') as f:
-                    f.write(f"done. {len(file_lines)} lines found. max line {max_line}")
+                            if '@@ -163,1137 +163,6 @@' in content or '-def control_camera' in content or '1137' in content:
+                                diff_content = content
+                                
+                with open("restored9.txt", "w", encoding='utf-8') as f:
+                    f.write(diff_content)
             except Exception as e:
-                with open("restored8.txt", "w") as f:
+                with open("restored9.txt", "w") as f:
                     f.write("error: " + str(e))
 
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
--- MATCH 10 ---
Created At: 2026-06-29T11:15:25Z
Completed At: 2026-06-29T11:15:28Z
The following changes were made by the replace_file_content tool to: c:\private\Project\SmartCCTV\dashboard\apps.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -5,22 +5,23 @@
     name = "dashboard"
 
     def ready(self):
-        if not os.path.exists("restored9.txt"):
-            import json, os
+        if not os.path.exists("restored10.txt"):
+            import json
             transcript_path = r"C:\Users\Admin\.gemini\antigravity-ide\brain\896911f3-b771-4e6a-976d-315f11c23b29\.system_generated\logs\transcript_full.jsonl"
             try:
-                diff_content = ""
+                matches = []
                 with open(transcript_path, 'r', encoding='utf-8') as f:
                     for line in f:
-                        if '"type":"TOOL_RESPONSE"' in line and '[diff_block_start]' in line and 'views.py' in line:
+                        if 'control_camera' in line:
                             data = json.loads(line)
                             content = data.get('content', '') or data.get('output', '')
-                            if '@@ -163,1137 +163,6 @@' in content or '-def control_camera' in content or '1137' in content:
-                                diff_content = content
+                            if content and 'def control_camera' in content:
+                                matches.append(content)
                                 
-                with open("restored9.txt", "w", encoding='utf-8') as f:
-                    f.write(diff_content)
+                with open("restored10.txt", "w", encoding='utf-8') as f:
+                    for i, match in enumerate(matches):
+                        f.write(f"--- MATCH {i} ---\n{match}\n")
             except Exception as e:
-                with open("restored9.txt", "w") as f:
+                with open("restored10.txt", "w") as f:
                     f.write("error: " + str(e))
 
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
